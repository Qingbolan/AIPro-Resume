// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"silan-backend/internal/ent/sociallink"
	"silan-backend/internal/ent/user"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// SocialLink is the model entity for the SocialLink schema.
type SocialLink struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// UserID holds the value of the "user_id" field.
	UserID uuid.UUID `json:"user_id,omitempty"`
	// Platform holds the value of the "platform" field.
	Platform string `json:"platform,omitempty"`
	// URL holds the value of the "url" field.
	URL string `json:"url,omitempty"`
	// Username holds the value of the "username" field.
	Username string `json:"username,omitempty"`
	// DisplayName holds the value of the "display_name" field.
	DisplayName string `json:"display_name,omitempty"`
	// IsPublic holds the value of the "is_public" field.
	IsPublic bool `json:"is_public,omitempty"`
	// Order holds the value of the "order" field.
	Order int `json:"order,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SocialLinkQuery when eager-loading is set.
	Edges             SocialLinkEdges `json:"edges"`
	user_social_links *uuid.UUID
	selectValues      sql.SelectValues
}

// SocialLinkEdges holds the relations/edges for other nodes in the graph.
type SocialLinkEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SocialLinkEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SocialLink) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case sociallink.FieldIsPublic:
			values[i] = new(sql.NullBool)
		case sociallink.FieldOrder:
			values[i] = new(sql.NullInt64)
		case sociallink.FieldPlatform, sociallink.FieldURL, sociallink.FieldUsername, sociallink.FieldDisplayName:
			values[i] = new(sql.NullString)
		case sociallink.FieldCreatedAt, sociallink.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case sociallink.FieldID, sociallink.FieldUserID:
			values[i] = new(uuid.UUID)
		case sociallink.ForeignKeys[0]: // user_social_links
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SocialLink fields.
func (sl *SocialLink) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case sociallink.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				sl.ID = *value
			}
		case sociallink.FieldUserID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value != nil {
				sl.UserID = *value
			}
		case sociallink.FieldPlatform:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field platform", values[i])
			} else if value.Valid {
				sl.Platform = value.String
			}
		case sociallink.FieldURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field url", values[i])
			} else if value.Valid {
				sl.URL = value.String
			}
		case sociallink.FieldUsername:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field username", values[i])
			} else if value.Valid {
				sl.Username = value.String
			}
		case sociallink.FieldDisplayName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field display_name", values[i])
			} else if value.Valid {
				sl.DisplayName = value.String
			}
		case sociallink.FieldIsPublic:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_public", values[i])
			} else if value.Valid {
				sl.IsPublic = value.Bool
			}
		case sociallink.FieldOrder:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field order", values[i])
			} else if value.Valid {
				sl.Order = int(value.Int64)
			}
		case sociallink.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				sl.CreatedAt = value.Time
			}
		case sociallink.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				sl.UpdatedAt = value.Time
			}
		case sociallink.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field user_social_links", values[i])
			} else if value.Valid {
				sl.user_social_links = new(uuid.UUID)
				*sl.user_social_links = *value.S.(*uuid.UUID)
			}
		default:
			sl.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SocialLink.
// This includes values selected through modifiers, order, etc.
func (sl *SocialLink) Value(name string) (ent.Value, error) {
	return sl.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the SocialLink entity.
func (sl *SocialLink) QueryUser() *UserQuery {
	return NewSocialLinkClient(sl.config).QueryUser(sl)
}

// Update returns a builder for updating this SocialLink.
// Note that you need to call SocialLink.Unwrap() before calling this method if this SocialLink
// was returned from a transaction, and the transaction was committed or rolled back.
func (sl *SocialLink) Update() *SocialLinkUpdateOne {
	return NewSocialLinkClient(sl.config).UpdateOne(sl)
}

// Unwrap unwraps the SocialLink entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sl *SocialLink) Unwrap() *SocialLink {
	_tx, ok := sl.config.driver.(*txDriver)
	if !ok {
		panic("ent: SocialLink is not a transactional entity")
	}
	sl.config.driver = _tx.drv
	return sl
}

// String implements the fmt.Stringer.
func (sl *SocialLink) String() string {
	var builder strings.Builder
	builder.WriteString("SocialLink(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sl.ID))
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", sl.UserID))
	builder.WriteString(", ")
	builder.WriteString("platform=")
	builder.WriteString(sl.Platform)
	builder.WriteString(", ")
	builder.WriteString("url=")
	builder.WriteString(sl.URL)
	builder.WriteString(", ")
	builder.WriteString("username=")
	builder.WriteString(sl.Username)
	builder.WriteString(", ")
	builder.WriteString("display_name=")
	builder.WriteString(sl.DisplayName)
	builder.WriteString(", ")
	builder.WriteString("is_public=")
	builder.WriteString(fmt.Sprintf("%v", sl.IsPublic))
	builder.WriteString(", ")
	builder.WriteString("order=")
	builder.WriteString(fmt.Sprintf("%v", sl.Order))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(sl.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(sl.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// SocialLinks is a parsable slice of SocialLink.
type SocialLinks []*SocialLink
