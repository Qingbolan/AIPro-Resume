// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"silan-backend/internal/ent/blogseries"
	"silan-backend/internal/ent/blogseriestranslation"
	"silan-backend/internal/ent/language"
	"silan-backend/internal/ent/predicate"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// BlogSeriesTranslationQuery is the builder for querying BlogSeriesTranslation entities.
type BlogSeriesTranslationQuery struct {
	config
	ctx            *QueryContext
	order          []blogseriestranslation.OrderOption
	inters         []Interceptor
	predicates     []predicate.BlogSeriesTranslation
	withBlogSeries *BlogSeriesQuery
	withLanguage   *LanguageQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the BlogSeriesTranslationQuery builder.
func (bstq *BlogSeriesTranslationQuery) Where(ps ...predicate.BlogSeriesTranslation) *BlogSeriesTranslationQuery {
	bstq.predicates = append(bstq.predicates, ps...)
	return bstq
}

// Limit the number of records to be returned by this query.
func (bstq *BlogSeriesTranslationQuery) Limit(limit int) *BlogSeriesTranslationQuery {
	bstq.ctx.Limit = &limit
	return bstq
}

// Offset to start from.
func (bstq *BlogSeriesTranslationQuery) Offset(offset int) *BlogSeriesTranslationQuery {
	bstq.ctx.Offset = &offset
	return bstq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (bstq *BlogSeriesTranslationQuery) Unique(unique bool) *BlogSeriesTranslationQuery {
	bstq.ctx.Unique = &unique
	return bstq
}

// Order specifies how the records should be ordered.
func (bstq *BlogSeriesTranslationQuery) Order(o ...blogseriestranslation.OrderOption) *BlogSeriesTranslationQuery {
	bstq.order = append(bstq.order, o...)
	return bstq
}

// QueryBlogSeries chains the current query on the "blog_series" edge.
func (bstq *BlogSeriesTranslationQuery) QueryBlogSeries() *BlogSeriesQuery {
	query := (&BlogSeriesClient{config: bstq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := bstq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := bstq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(blogseriestranslation.Table, blogseriestranslation.FieldID, selector),
			sqlgraph.To(blogseries.Table, blogseries.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, blogseriestranslation.BlogSeriesTable, blogseriestranslation.BlogSeriesColumn),
		)
		fromU = sqlgraph.SetNeighbors(bstq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryLanguage chains the current query on the "language" edge.
func (bstq *BlogSeriesTranslationQuery) QueryLanguage() *LanguageQuery {
	query := (&LanguageClient{config: bstq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := bstq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := bstq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(blogseriestranslation.Table, blogseriestranslation.FieldID, selector),
			sqlgraph.To(language.Table, language.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, blogseriestranslation.LanguageTable, blogseriestranslation.LanguageColumn),
		)
		fromU = sqlgraph.SetNeighbors(bstq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first BlogSeriesTranslation entity from the query.
// Returns a *NotFoundError when no BlogSeriesTranslation was found.
func (bstq *BlogSeriesTranslationQuery) First(ctx context.Context) (*BlogSeriesTranslation, error) {
	nodes, err := bstq.Limit(1).All(setContextOp(ctx, bstq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{blogseriestranslation.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (bstq *BlogSeriesTranslationQuery) FirstX(ctx context.Context) *BlogSeriesTranslation {
	node, err := bstq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first BlogSeriesTranslation ID from the query.
// Returns a *NotFoundError when no BlogSeriesTranslation ID was found.
func (bstq *BlogSeriesTranslationQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = bstq.Limit(1).IDs(setContextOp(ctx, bstq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{blogseriestranslation.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (bstq *BlogSeriesTranslationQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := bstq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single BlogSeriesTranslation entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one BlogSeriesTranslation entity is found.
// Returns a *NotFoundError when no BlogSeriesTranslation entities are found.
func (bstq *BlogSeriesTranslationQuery) Only(ctx context.Context) (*BlogSeriesTranslation, error) {
	nodes, err := bstq.Limit(2).All(setContextOp(ctx, bstq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{blogseriestranslation.Label}
	default:
		return nil, &NotSingularError{blogseriestranslation.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (bstq *BlogSeriesTranslationQuery) OnlyX(ctx context.Context) *BlogSeriesTranslation {
	node, err := bstq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only BlogSeriesTranslation ID in the query.
// Returns a *NotSingularError when more than one BlogSeriesTranslation ID is found.
// Returns a *NotFoundError when no entities are found.
func (bstq *BlogSeriesTranslationQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = bstq.Limit(2).IDs(setContextOp(ctx, bstq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{blogseriestranslation.Label}
	default:
		err = &NotSingularError{blogseriestranslation.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (bstq *BlogSeriesTranslationQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := bstq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of BlogSeriesTranslations.
func (bstq *BlogSeriesTranslationQuery) All(ctx context.Context) ([]*BlogSeriesTranslation, error) {
	ctx = setContextOp(ctx, bstq.ctx, ent.OpQueryAll)
	if err := bstq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*BlogSeriesTranslation, *BlogSeriesTranslationQuery]()
	return withInterceptors[[]*BlogSeriesTranslation](ctx, bstq, qr, bstq.inters)
}

// AllX is like All, but panics if an error occurs.
func (bstq *BlogSeriesTranslationQuery) AllX(ctx context.Context) []*BlogSeriesTranslation {
	nodes, err := bstq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of BlogSeriesTranslation IDs.
func (bstq *BlogSeriesTranslationQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if bstq.ctx.Unique == nil && bstq.path != nil {
		bstq.Unique(true)
	}
	ctx = setContextOp(ctx, bstq.ctx, ent.OpQueryIDs)
	if err = bstq.Select(blogseriestranslation.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (bstq *BlogSeriesTranslationQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := bstq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (bstq *BlogSeriesTranslationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, bstq.ctx, ent.OpQueryCount)
	if err := bstq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, bstq, querierCount[*BlogSeriesTranslationQuery](), bstq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (bstq *BlogSeriesTranslationQuery) CountX(ctx context.Context) int {
	count, err := bstq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (bstq *BlogSeriesTranslationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, bstq.ctx, ent.OpQueryExist)
	switch _, err := bstq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (bstq *BlogSeriesTranslationQuery) ExistX(ctx context.Context) bool {
	exist, err := bstq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the BlogSeriesTranslationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (bstq *BlogSeriesTranslationQuery) Clone() *BlogSeriesTranslationQuery {
	if bstq == nil {
		return nil
	}
	return &BlogSeriesTranslationQuery{
		config:         bstq.config,
		ctx:            bstq.ctx.Clone(),
		order:          append([]blogseriestranslation.OrderOption{}, bstq.order...),
		inters:         append([]Interceptor{}, bstq.inters...),
		predicates:     append([]predicate.BlogSeriesTranslation{}, bstq.predicates...),
		withBlogSeries: bstq.withBlogSeries.Clone(),
		withLanguage:   bstq.withLanguage.Clone(),
		// clone intermediate query.
		sql:  bstq.sql.Clone(),
		path: bstq.path,
	}
}

// WithBlogSeries tells the query-builder to eager-load the nodes that are connected to
// the "blog_series" edge. The optional arguments are used to configure the query builder of the edge.
func (bstq *BlogSeriesTranslationQuery) WithBlogSeries(opts ...func(*BlogSeriesQuery)) *BlogSeriesTranslationQuery {
	query := (&BlogSeriesClient{config: bstq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	bstq.withBlogSeries = query
	return bstq
}

// WithLanguage tells the query-builder to eager-load the nodes that are connected to
// the "language" edge. The optional arguments are used to configure the query builder of the edge.
func (bstq *BlogSeriesTranslationQuery) WithLanguage(opts ...func(*LanguageQuery)) *BlogSeriesTranslationQuery {
	query := (&LanguageClient{config: bstq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	bstq.withLanguage = query
	return bstq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		BlogSeriesID uuid.UUID `json:"blog_series_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.BlogSeriesTranslation.Query().
//		GroupBy(blogseriestranslation.FieldBlogSeriesID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (bstq *BlogSeriesTranslationQuery) GroupBy(field string, fields ...string) *BlogSeriesTranslationGroupBy {
	bstq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &BlogSeriesTranslationGroupBy{build: bstq}
	grbuild.flds = &bstq.ctx.Fields
	grbuild.label = blogseriestranslation.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		BlogSeriesID uuid.UUID `json:"blog_series_id,omitempty"`
//	}
//
//	client.BlogSeriesTranslation.Query().
//		Select(blogseriestranslation.FieldBlogSeriesID).
//		Scan(ctx, &v)
func (bstq *BlogSeriesTranslationQuery) Select(fields ...string) *BlogSeriesTranslationSelect {
	bstq.ctx.Fields = append(bstq.ctx.Fields, fields...)
	sbuild := &BlogSeriesTranslationSelect{BlogSeriesTranslationQuery: bstq}
	sbuild.label = blogseriestranslation.Label
	sbuild.flds, sbuild.scan = &bstq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a BlogSeriesTranslationSelect configured with the given aggregations.
func (bstq *BlogSeriesTranslationQuery) Aggregate(fns ...AggregateFunc) *BlogSeriesTranslationSelect {
	return bstq.Select().Aggregate(fns...)
}

func (bstq *BlogSeriesTranslationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range bstq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, bstq); err != nil {
				return err
			}
		}
	}
	for _, f := range bstq.ctx.Fields {
		if !blogseriestranslation.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if bstq.path != nil {
		prev, err := bstq.path(ctx)
		if err != nil {
			return err
		}
		bstq.sql = prev
	}
	return nil
}

func (bstq *BlogSeriesTranslationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*BlogSeriesTranslation, error) {
	var (
		nodes       = []*BlogSeriesTranslation{}
		_spec       = bstq.querySpec()
		loadedTypes = [2]bool{
			bstq.withBlogSeries != nil,
			bstq.withLanguage != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*BlogSeriesTranslation).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &BlogSeriesTranslation{config: bstq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, bstq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := bstq.withBlogSeries; query != nil {
		if err := bstq.loadBlogSeries(ctx, query, nodes, nil,
			func(n *BlogSeriesTranslation, e *BlogSeries) { n.Edges.BlogSeries = e }); err != nil {
			return nil, err
		}
	}
	if query := bstq.withLanguage; query != nil {
		if err := bstq.loadLanguage(ctx, query, nodes, nil,
			func(n *BlogSeriesTranslation, e *Language) { n.Edges.Language = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (bstq *BlogSeriesTranslationQuery) loadBlogSeries(ctx context.Context, query *BlogSeriesQuery, nodes []*BlogSeriesTranslation, init func(*BlogSeriesTranslation), assign func(*BlogSeriesTranslation, *BlogSeries)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*BlogSeriesTranslation)
	for i := range nodes {
		fk := nodes[i].BlogSeriesID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(blogseries.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "blog_series_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (bstq *BlogSeriesTranslationQuery) loadLanguage(ctx context.Context, query *LanguageQuery, nodes []*BlogSeriesTranslation, init func(*BlogSeriesTranslation), assign func(*BlogSeriesTranslation, *Language)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*BlogSeriesTranslation)
	for i := range nodes {
		fk := nodes[i].LanguageCode
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(language.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "language_code" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (bstq *BlogSeriesTranslationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := bstq.querySpec()
	_spec.Node.Columns = bstq.ctx.Fields
	if len(bstq.ctx.Fields) > 0 {
		_spec.Unique = bstq.ctx.Unique != nil && *bstq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, bstq.driver, _spec)
}

func (bstq *BlogSeriesTranslationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(blogseriestranslation.Table, blogseriestranslation.Columns, sqlgraph.NewFieldSpec(blogseriestranslation.FieldID, field.TypeUUID))
	_spec.From = bstq.sql
	if unique := bstq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if bstq.path != nil {
		_spec.Unique = true
	}
	if fields := bstq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, blogseriestranslation.FieldID)
		for i := range fields {
			if fields[i] != blogseriestranslation.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if bstq.withBlogSeries != nil {
			_spec.Node.AddColumnOnce(blogseriestranslation.FieldBlogSeriesID)
		}
		if bstq.withLanguage != nil {
			_spec.Node.AddColumnOnce(blogseriestranslation.FieldLanguageCode)
		}
	}
	if ps := bstq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := bstq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := bstq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := bstq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (bstq *BlogSeriesTranslationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(bstq.driver.Dialect())
	t1 := builder.Table(blogseriestranslation.Table)
	columns := bstq.ctx.Fields
	if len(columns) == 0 {
		columns = blogseriestranslation.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if bstq.sql != nil {
		selector = bstq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if bstq.ctx.Unique != nil && *bstq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range bstq.predicates {
		p(selector)
	}
	for _, p := range bstq.order {
		p(selector)
	}
	if offset := bstq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := bstq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// BlogSeriesTranslationGroupBy is the group-by builder for BlogSeriesTranslation entities.
type BlogSeriesTranslationGroupBy struct {
	selector
	build *BlogSeriesTranslationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (bstgb *BlogSeriesTranslationGroupBy) Aggregate(fns ...AggregateFunc) *BlogSeriesTranslationGroupBy {
	bstgb.fns = append(bstgb.fns, fns...)
	return bstgb
}

// Scan applies the selector query and scans the result into the given value.
func (bstgb *BlogSeriesTranslationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, bstgb.build.ctx, ent.OpQueryGroupBy)
	if err := bstgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BlogSeriesTranslationQuery, *BlogSeriesTranslationGroupBy](ctx, bstgb.build, bstgb, bstgb.build.inters, v)
}

func (bstgb *BlogSeriesTranslationGroupBy) sqlScan(ctx context.Context, root *BlogSeriesTranslationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(bstgb.fns))
	for _, fn := range bstgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*bstgb.flds)+len(bstgb.fns))
		for _, f := range *bstgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*bstgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := bstgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// BlogSeriesTranslationSelect is the builder for selecting fields of BlogSeriesTranslation entities.
type BlogSeriesTranslationSelect struct {
	*BlogSeriesTranslationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (bsts *BlogSeriesTranslationSelect) Aggregate(fns ...AggregateFunc) *BlogSeriesTranslationSelect {
	bsts.fns = append(bsts.fns, fns...)
	return bsts
}

// Scan applies the selector query and scans the result into the given value.
func (bsts *BlogSeriesTranslationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, bsts.ctx, ent.OpQuerySelect)
	if err := bsts.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BlogSeriesTranslationQuery, *BlogSeriesTranslationSelect](ctx, bsts.BlogSeriesTranslationQuery, bsts, bsts.inters, v)
}

func (bsts *BlogSeriesTranslationSelect) sqlScan(ctx context.Context, root *BlogSeriesTranslationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(bsts.fns))
	for _, fn := range bsts.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*bsts.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := bsts.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
