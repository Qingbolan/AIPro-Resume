// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"silan-backend/internal/ent/predicate"
	"silan-backend/internal/ent/project"
	"silan-backend/internal/ent/projectrelationship"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// ProjectRelationshipQuery is the builder for querying ProjectRelationship entities.
type ProjectRelationshipQuery struct {
	config
	ctx               *QueryContext
	order             []projectrelationship.OrderOption
	inters            []Interceptor
	predicates        []predicate.ProjectRelationship
	withSourceProject *ProjectQuery
	withTargetProject *ProjectQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ProjectRelationshipQuery builder.
func (prq *ProjectRelationshipQuery) Where(ps ...predicate.ProjectRelationship) *ProjectRelationshipQuery {
	prq.predicates = append(prq.predicates, ps...)
	return prq
}

// Limit the number of records to be returned by this query.
func (prq *ProjectRelationshipQuery) Limit(limit int) *ProjectRelationshipQuery {
	prq.ctx.Limit = &limit
	return prq
}

// Offset to start from.
func (prq *ProjectRelationshipQuery) Offset(offset int) *ProjectRelationshipQuery {
	prq.ctx.Offset = &offset
	return prq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (prq *ProjectRelationshipQuery) Unique(unique bool) *ProjectRelationshipQuery {
	prq.ctx.Unique = &unique
	return prq
}

// Order specifies how the records should be ordered.
func (prq *ProjectRelationshipQuery) Order(o ...projectrelationship.OrderOption) *ProjectRelationshipQuery {
	prq.order = append(prq.order, o...)
	return prq
}

// QuerySourceProject chains the current query on the "source_project" edge.
func (prq *ProjectRelationshipQuery) QuerySourceProject() *ProjectQuery {
	query := (&ProjectClient{config: prq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := prq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := prq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(projectrelationship.Table, projectrelationship.FieldID, selector),
			sqlgraph.To(project.Table, project.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, projectrelationship.SourceProjectTable, projectrelationship.SourceProjectColumn),
		)
		fromU = sqlgraph.SetNeighbors(prq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTargetProject chains the current query on the "target_project" edge.
func (prq *ProjectRelationshipQuery) QueryTargetProject() *ProjectQuery {
	query := (&ProjectClient{config: prq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := prq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := prq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(projectrelationship.Table, projectrelationship.FieldID, selector),
			sqlgraph.To(project.Table, project.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, projectrelationship.TargetProjectTable, projectrelationship.TargetProjectColumn),
		)
		fromU = sqlgraph.SetNeighbors(prq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ProjectRelationship entity from the query.
// Returns a *NotFoundError when no ProjectRelationship was found.
func (prq *ProjectRelationshipQuery) First(ctx context.Context) (*ProjectRelationship, error) {
	nodes, err := prq.Limit(1).All(setContextOp(ctx, prq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{projectrelationship.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (prq *ProjectRelationshipQuery) FirstX(ctx context.Context) *ProjectRelationship {
	node, err := prq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ProjectRelationship ID from the query.
// Returns a *NotFoundError when no ProjectRelationship ID was found.
func (prq *ProjectRelationshipQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = prq.Limit(1).IDs(setContextOp(ctx, prq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{projectrelationship.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (prq *ProjectRelationshipQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := prq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ProjectRelationship entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ProjectRelationship entity is found.
// Returns a *NotFoundError when no ProjectRelationship entities are found.
func (prq *ProjectRelationshipQuery) Only(ctx context.Context) (*ProjectRelationship, error) {
	nodes, err := prq.Limit(2).All(setContextOp(ctx, prq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{projectrelationship.Label}
	default:
		return nil, &NotSingularError{projectrelationship.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (prq *ProjectRelationshipQuery) OnlyX(ctx context.Context) *ProjectRelationship {
	node, err := prq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ProjectRelationship ID in the query.
// Returns a *NotSingularError when more than one ProjectRelationship ID is found.
// Returns a *NotFoundError when no entities are found.
func (prq *ProjectRelationshipQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = prq.Limit(2).IDs(setContextOp(ctx, prq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{projectrelationship.Label}
	default:
		err = &NotSingularError{projectrelationship.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (prq *ProjectRelationshipQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := prq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ProjectRelationships.
func (prq *ProjectRelationshipQuery) All(ctx context.Context) ([]*ProjectRelationship, error) {
	ctx = setContextOp(ctx, prq.ctx, ent.OpQueryAll)
	if err := prq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ProjectRelationship, *ProjectRelationshipQuery]()
	return withInterceptors[[]*ProjectRelationship](ctx, prq, qr, prq.inters)
}

// AllX is like All, but panics if an error occurs.
func (prq *ProjectRelationshipQuery) AllX(ctx context.Context) []*ProjectRelationship {
	nodes, err := prq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ProjectRelationship IDs.
func (prq *ProjectRelationshipQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if prq.ctx.Unique == nil && prq.path != nil {
		prq.Unique(true)
	}
	ctx = setContextOp(ctx, prq.ctx, ent.OpQueryIDs)
	if err = prq.Select(projectrelationship.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (prq *ProjectRelationshipQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := prq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (prq *ProjectRelationshipQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, prq.ctx, ent.OpQueryCount)
	if err := prq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, prq, querierCount[*ProjectRelationshipQuery](), prq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (prq *ProjectRelationshipQuery) CountX(ctx context.Context) int {
	count, err := prq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (prq *ProjectRelationshipQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, prq.ctx, ent.OpQueryExist)
	switch _, err := prq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (prq *ProjectRelationshipQuery) ExistX(ctx context.Context) bool {
	exist, err := prq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ProjectRelationshipQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (prq *ProjectRelationshipQuery) Clone() *ProjectRelationshipQuery {
	if prq == nil {
		return nil
	}
	return &ProjectRelationshipQuery{
		config:            prq.config,
		ctx:               prq.ctx.Clone(),
		order:             append([]projectrelationship.OrderOption{}, prq.order...),
		inters:            append([]Interceptor{}, prq.inters...),
		predicates:        append([]predicate.ProjectRelationship{}, prq.predicates...),
		withSourceProject: prq.withSourceProject.Clone(),
		withTargetProject: prq.withTargetProject.Clone(),
		// clone intermediate query.
		sql:  prq.sql.Clone(),
		path: prq.path,
	}
}

// WithSourceProject tells the query-builder to eager-load the nodes that are connected to
// the "source_project" edge. The optional arguments are used to configure the query builder of the edge.
func (prq *ProjectRelationshipQuery) WithSourceProject(opts ...func(*ProjectQuery)) *ProjectRelationshipQuery {
	query := (&ProjectClient{config: prq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	prq.withSourceProject = query
	return prq
}

// WithTargetProject tells the query-builder to eager-load the nodes that are connected to
// the "target_project" edge. The optional arguments are used to configure the query builder of the edge.
func (prq *ProjectRelationshipQuery) WithTargetProject(opts ...func(*ProjectQuery)) *ProjectRelationshipQuery {
	query := (&ProjectClient{config: prq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	prq.withTargetProject = query
	return prq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		SourceProjectID uuid.UUID `json:"source_project_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ProjectRelationship.Query().
//		GroupBy(projectrelationship.FieldSourceProjectID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (prq *ProjectRelationshipQuery) GroupBy(field string, fields ...string) *ProjectRelationshipGroupBy {
	prq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ProjectRelationshipGroupBy{build: prq}
	grbuild.flds = &prq.ctx.Fields
	grbuild.label = projectrelationship.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		SourceProjectID uuid.UUID `json:"source_project_id,omitempty"`
//	}
//
//	client.ProjectRelationship.Query().
//		Select(projectrelationship.FieldSourceProjectID).
//		Scan(ctx, &v)
func (prq *ProjectRelationshipQuery) Select(fields ...string) *ProjectRelationshipSelect {
	prq.ctx.Fields = append(prq.ctx.Fields, fields...)
	sbuild := &ProjectRelationshipSelect{ProjectRelationshipQuery: prq}
	sbuild.label = projectrelationship.Label
	sbuild.flds, sbuild.scan = &prq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ProjectRelationshipSelect configured with the given aggregations.
func (prq *ProjectRelationshipQuery) Aggregate(fns ...AggregateFunc) *ProjectRelationshipSelect {
	return prq.Select().Aggregate(fns...)
}

func (prq *ProjectRelationshipQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range prq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, prq); err != nil {
				return err
			}
		}
	}
	for _, f := range prq.ctx.Fields {
		if !projectrelationship.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if prq.path != nil {
		prev, err := prq.path(ctx)
		if err != nil {
			return err
		}
		prq.sql = prev
	}
	return nil
}

func (prq *ProjectRelationshipQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ProjectRelationship, error) {
	var (
		nodes       = []*ProjectRelationship{}
		_spec       = prq.querySpec()
		loadedTypes = [2]bool{
			prq.withSourceProject != nil,
			prq.withTargetProject != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ProjectRelationship).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ProjectRelationship{config: prq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, prq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := prq.withSourceProject; query != nil {
		if err := prq.loadSourceProject(ctx, query, nodes, nil,
			func(n *ProjectRelationship, e *Project) { n.Edges.SourceProject = e }); err != nil {
			return nil, err
		}
	}
	if query := prq.withTargetProject; query != nil {
		if err := prq.loadTargetProject(ctx, query, nodes, nil,
			func(n *ProjectRelationship, e *Project) { n.Edges.TargetProject = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (prq *ProjectRelationshipQuery) loadSourceProject(ctx context.Context, query *ProjectQuery, nodes []*ProjectRelationship, init func(*ProjectRelationship), assign func(*ProjectRelationship, *Project)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ProjectRelationship)
	for i := range nodes {
		fk := nodes[i].SourceProjectID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(project.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "source_project_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (prq *ProjectRelationshipQuery) loadTargetProject(ctx context.Context, query *ProjectQuery, nodes []*ProjectRelationship, init func(*ProjectRelationship), assign func(*ProjectRelationship, *Project)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ProjectRelationship)
	for i := range nodes {
		fk := nodes[i].TargetProjectID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(project.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "target_project_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (prq *ProjectRelationshipQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := prq.querySpec()
	_spec.Node.Columns = prq.ctx.Fields
	if len(prq.ctx.Fields) > 0 {
		_spec.Unique = prq.ctx.Unique != nil && *prq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, prq.driver, _spec)
}

func (prq *ProjectRelationshipQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(projectrelationship.Table, projectrelationship.Columns, sqlgraph.NewFieldSpec(projectrelationship.FieldID, field.TypeUUID))
	_spec.From = prq.sql
	if unique := prq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if prq.path != nil {
		_spec.Unique = true
	}
	if fields := prq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, projectrelationship.FieldID)
		for i := range fields {
			if fields[i] != projectrelationship.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if prq.withSourceProject != nil {
			_spec.Node.AddColumnOnce(projectrelationship.FieldSourceProjectID)
		}
		if prq.withTargetProject != nil {
			_spec.Node.AddColumnOnce(projectrelationship.FieldTargetProjectID)
		}
	}
	if ps := prq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := prq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := prq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := prq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (prq *ProjectRelationshipQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(prq.driver.Dialect())
	t1 := builder.Table(projectrelationship.Table)
	columns := prq.ctx.Fields
	if len(columns) == 0 {
		columns = projectrelationship.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if prq.sql != nil {
		selector = prq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if prq.ctx.Unique != nil && *prq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range prq.predicates {
		p(selector)
	}
	for _, p := range prq.order {
		p(selector)
	}
	if offset := prq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := prq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ProjectRelationshipGroupBy is the group-by builder for ProjectRelationship entities.
type ProjectRelationshipGroupBy struct {
	selector
	build *ProjectRelationshipQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (prgb *ProjectRelationshipGroupBy) Aggregate(fns ...AggregateFunc) *ProjectRelationshipGroupBy {
	prgb.fns = append(prgb.fns, fns...)
	return prgb
}

// Scan applies the selector query and scans the result into the given value.
func (prgb *ProjectRelationshipGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, prgb.build.ctx, ent.OpQueryGroupBy)
	if err := prgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProjectRelationshipQuery, *ProjectRelationshipGroupBy](ctx, prgb.build, prgb, prgb.build.inters, v)
}

func (prgb *ProjectRelationshipGroupBy) sqlScan(ctx context.Context, root *ProjectRelationshipQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(prgb.fns))
	for _, fn := range prgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*prgb.flds)+len(prgb.fns))
		for _, f := range *prgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*prgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := prgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ProjectRelationshipSelect is the builder for selecting fields of ProjectRelationship entities.
type ProjectRelationshipSelect struct {
	*ProjectRelationshipQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (prs *ProjectRelationshipSelect) Aggregate(fns ...AggregateFunc) *ProjectRelationshipSelect {
	prs.fns = append(prs.fns, fns...)
	return prs
}

// Scan applies the selector query and scans the result into the given value.
func (prs *ProjectRelationshipSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, prs.ctx, ent.OpQuerySelect)
	if err := prs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProjectRelationshipQuery, *ProjectRelationshipSelect](ctx, prs.ProjectRelationshipQuery, prs, prs.inters, v)
}

func (prs *ProjectRelationshipSelect) sqlScan(ctx context.Context, root *ProjectRelationshipQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(prs.fns))
	for _, fn := range prs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*prs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := prs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
