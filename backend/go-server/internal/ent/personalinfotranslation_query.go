// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"silan-backend/internal/ent/language"
	"silan-backend/internal/ent/personalinfo"
	"silan-backend/internal/ent/personalinfotranslation"
	"silan-backend/internal/ent/predicate"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// PersonalInfoTranslationQuery is the builder for querying PersonalInfoTranslation entities.
type PersonalInfoTranslationQuery struct {
	config
	ctx              *QueryContext
	order            []personalinfotranslation.OrderOption
	inters           []Interceptor
	predicates       []predicate.PersonalInfoTranslation
	withPersonalInfo *PersonalInfoQuery
	withLanguage     *LanguageQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PersonalInfoTranslationQuery builder.
func (pitq *PersonalInfoTranslationQuery) Where(ps ...predicate.PersonalInfoTranslation) *PersonalInfoTranslationQuery {
	pitq.predicates = append(pitq.predicates, ps...)
	return pitq
}

// Limit the number of records to be returned by this query.
func (pitq *PersonalInfoTranslationQuery) Limit(limit int) *PersonalInfoTranslationQuery {
	pitq.ctx.Limit = &limit
	return pitq
}

// Offset to start from.
func (pitq *PersonalInfoTranslationQuery) Offset(offset int) *PersonalInfoTranslationQuery {
	pitq.ctx.Offset = &offset
	return pitq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pitq *PersonalInfoTranslationQuery) Unique(unique bool) *PersonalInfoTranslationQuery {
	pitq.ctx.Unique = &unique
	return pitq
}

// Order specifies how the records should be ordered.
func (pitq *PersonalInfoTranslationQuery) Order(o ...personalinfotranslation.OrderOption) *PersonalInfoTranslationQuery {
	pitq.order = append(pitq.order, o...)
	return pitq
}

// QueryPersonalInfo chains the current query on the "personal_info" edge.
func (pitq *PersonalInfoTranslationQuery) QueryPersonalInfo() *PersonalInfoQuery {
	query := (&PersonalInfoClient{config: pitq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pitq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pitq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(personalinfotranslation.Table, personalinfotranslation.FieldID, selector),
			sqlgraph.To(personalinfo.Table, personalinfo.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, personalinfotranslation.PersonalInfoTable, personalinfotranslation.PersonalInfoColumn),
		)
		fromU = sqlgraph.SetNeighbors(pitq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryLanguage chains the current query on the "language" edge.
func (pitq *PersonalInfoTranslationQuery) QueryLanguage() *LanguageQuery {
	query := (&LanguageClient{config: pitq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pitq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pitq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(personalinfotranslation.Table, personalinfotranslation.FieldID, selector),
			sqlgraph.To(language.Table, language.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, personalinfotranslation.LanguageTable, personalinfotranslation.LanguageColumn),
		)
		fromU = sqlgraph.SetNeighbors(pitq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first PersonalInfoTranslation entity from the query.
// Returns a *NotFoundError when no PersonalInfoTranslation was found.
func (pitq *PersonalInfoTranslationQuery) First(ctx context.Context) (*PersonalInfoTranslation, error) {
	nodes, err := pitq.Limit(1).All(setContextOp(ctx, pitq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{personalinfotranslation.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pitq *PersonalInfoTranslationQuery) FirstX(ctx context.Context) *PersonalInfoTranslation {
	node, err := pitq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PersonalInfoTranslation ID from the query.
// Returns a *NotFoundError when no PersonalInfoTranslation ID was found.
func (pitq *PersonalInfoTranslationQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = pitq.Limit(1).IDs(setContextOp(ctx, pitq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{personalinfotranslation.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pitq *PersonalInfoTranslationQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := pitq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PersonalInfoTranslation entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PersonalInfoTranslation entity is found.
// Returns a *NotFoundError when no PersonalInfoTranslation entities are found.
func (pitq *PersonalInfoTranslationQuery) Only(ctx context.Context) (*PersonalInfoTranslation, error) {
	nodes, err := pitq.Limit(2).All(setContextOp(ctx, pitq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{personalinfotranslation.Label}
	default:
		return nil, &NotSingularError{personalinfotranslation.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pitq *PersonalInfoTranslationQuery) OnlyX(ctx context.Context) *PersonalInfoTranslation {
	node, err := pitq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PersonalInfoTranslation ID in the query.
// Returns a *NotSingularError when more than one PersonalInfoTranslation ID is found.
// Returns a *NotFoundError when no entities are found.
func (pitq *PersonalInfoTranslationQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = pitq.Limit(2).IDs(setContextOp(ctx, pitq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{personalinfotranslation.Label}
	default:
		err = &NotSingularError{personalinfotranslation.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pitq *PersonalInfoTranslationQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := pitq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PersonalInfoTranslations.
func (pitq *PersonalInfoTranslationQuery) All(ctx context.Context) ([]*PersonalInfoTranslation, error) {
	ctx = setContextOp(ctx, pitq.ctx, ent.OpQueryAll)
	if err := pitq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PersonalInfoTranslation, *PersonalInfoTranslationQuery]()
	return withInterceptors[[]*PersonalInfoTranslation](ctx, pitq, qr, pitq.inters)
}

// AllX is like All, but panics if an error occurs.
func (pitq *PersonalInfoTranslationQuery) AllX(ctx context.Context) []*PersonalInfoTranslation {
	nodes, err := pitq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PersonalInfoTranslation IDs.
func (pitq *PersonalInfoTranslationQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if pitq.ctx.Unique == nil && pitq.path != nil {
		pitq.Unique(true)
	}
	ctx = setContextOp(ctx, pitq.ctx, ent.OpQueryIDs)
	if err = pitq.Select(personalinfotranslation.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pitq *PersonalInfoTranslationQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := pitq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pitq *PersonalInfoTranslationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, pitq.ctx, ent.OpQueryCount)
	if err := pitq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, pitq, querierCount[*PersonalInfoTranslationQuery](), pitq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (pitq *PersonalInfoTranslationQuery) CountX(ctx context.Context) int {
	count, err := pitq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pitq *PersonalInfoTranslationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, pitq.ctx, ent.OpQueryExist)
	switch _, err := pitq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (pitq *PersonalInfoTranslationQuery) ExistX(ctx context.Context) bool {
	exist, err := pitq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PersonalInfoTranslationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pitq *PersonalInfoTranslationQuery) Clone() *PersonalInfoTranslationQuery {
	if pitq == nil {
		return nil
	}
	return &PersonalInfoTranslationQuery{
		config:           pitq.config,
		ctx:              pitq.ctx.Clone(),
		order:            append([]personalinfotranslation.OrderOption{}, pitq.order...),
		inters:           append([]Interceptor{}, pitq.inters...),
		predicates:       append([]predicate.PersonalInfoTranslation{}, pitq.predicates...),
		withPersonalInfo: pitq.withPersonalInfo.Clone(),
		withLanguage:     pitq.withLanguage.Clone(),
		// clone intermediate query.
		sql:  pitq.sql.Clone(),
		path: pitq.path,
	}
}

// WithPersonalInfo tells the query-builder to eager-load the nodes that are connected to
// the "personal_info" edge. The optional arguments are used to configure the query builder of the edge.
func (pitq *PersonalInfoTranslationQuery) WithPersonalInfo(opts ...func(*PersonalInfoQuery)) *PersonalInfoTranslationQuery {
	query := (&PersonalInfoClient{config: pitq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pitq.withPersonalInfo = query
	return pitq
}

// WithLanguage tells the query-builder to eager-load the nodes that are connected to
// the "language" edge. The optional arguments are used to configure the query builder of the edge.
func (pitq *PersonalInfoTranslationQuery) WithLanguage(opts ...func(*LanguageQuery)) *PersonalInfoTranslationQuery {
	query := (&LanguageClient{config: pitq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pitq.withLanguage = query
	return pitq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		PersonalInfoID uuid.UUID `json:"personal_info_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PersonalInfoTranslation.Query().
//		GroupBy(personalinfotranslation.FieldPersonalInfoID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (pitq *PersonalInfoTranslationQuery) GroupBy(field string, fields ...string) *PersonalInfoTranslationGroupBy {
	pitq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PersonalInfoTranslationGroupBy{build: pitq}
	grbuild.flds = &pitq.ctx.Fields
	grbuild.label = personalinfotranslation.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		PersonalInfoID uuid.UUID `json:"personal_info_id,omitempty"`
//	}
//
//	client.PersonalInfoTranslation.Query().
//		Select(personalinfotranslation.FieldPersonalInfoID).
//		Scan(ctx, &v)
func (pitq *PersonalInfoTranslationQuery) Select(fields ...string) *PersonalInfoTranslationSelect {
	pitq.ctx.Fields = append(pitq.ctx.Fields, fields...)
	sbuild := &PersonalInfoTranslationSelect{PersonalInfoTranslationQuery: pitq}
	sbuild.label = personalinfotranslation.Label
	sbuild.flds, sbuild.scan = &pitq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PersonalInfoTranslationSelect configured with the given aggregations.
func (pitq *PersonalInfoTranslationQuery) Aggregate(fns ...AggregateFunc) *PersonalInfoTranslationSelect {
	return pitq.Select().Aggregate(fns...)
}

func (pitq *PersonalInfoTranslationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range pitq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, pitq); err != nil {
				return err
			}
		}
	}
	for _, f := range pitq.ctx.Fields {
		if !personalinfotranslation.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pitq.path != nil {
		prev, err := pitq.path(ctx)
		if err != nil {
			return err
		}
		pitq.sql = prev
	}
	return nil
}

func (pitq *PersonalInfoTranslationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PersonalInfoTranslation, error) {
	var (
		nodes       = []*PersonalInfoTranslation{}
		_spec       = pitq.querySpec()
		loadedTypes = [2]bool{
			pitq.withPersonalInfo != nil,
			pitq.withLanguage != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PersonalInfoTranslation).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PersonalInfoTranslation{config: pitq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pitq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := pitq.withPersonalInfo; query != nil {
		if err := pitq.loadPersonalInfo(ctx, query, nodes, nil,
			func(n *PersonalInfoTranslation, e *PersonalInfo) { n.Edges.PersonalInfo = e }); err != nil {
			return nil, err
		}
	}
	if query := pitq.withLanguage; query != nil {
		if err := pitq.loadLanguage(ctx, query, nodes, nil,
			func(n *PersonalInfoTranslation, e *Language) { n.Edges.Language = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (pitq *PersonalInfoTranslationQuery) loadPersonalInfo(ctx context.Context, query *PersonalInfoQuery, nodes []*PersonalInfoTranslation, init func(*PersonalInfoTranslation), assign func(*PersonalInfoTranslation, *PersonalInfo)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*PersonalInfoTranslation)
	for i := range nodes {
		fk := nodes[i].PersonalInfoID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(personalinfo.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "personal_info_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (pitq *PersonalInfoTranslationQuery) loadLanguage(ctx context.Context, query *LanguageQuery, nodes []*PersonalInfoTranslation, init func(*PersonalInfoTranslation), assign func(*PersonalInfoTranslation, *Language)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*PersonalInfoTranslation)
	for i := range nodes {
		fk := nodes[i].LanguageCode
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(language.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "language_code" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (pitq *PersonalInfoTranslationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pitq.querySpec()
	_spec.Node.Columns = pitq.ctx.Fields
	if len(pitq.ctx.Fields) > 0 {
		_spec.Unique = pitq.ctx.Unique != nil && *pitq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, pitq.driver, _spec)
}

func (pitq *PersonalInfoTranslationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(personalinfotranslation.Table, personalinfotranslation.Columns, sqlgraph.NewFieldSpec(personalinfotranslation.FieldID, field.TypeUUID))
	_spec.From = pitq.sql
	if unique := pitq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if pitq.path != nil {
		_spec.Unique = true
	}
	if fields := pitq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, personalinfotranslation.FieldID)
		for i := range fields {
			if fields[i] != personalinfotranslation.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if pitq.withPersonalInfo != nil {
			_spec.Node.AddColumnOnce(personalinfotranslation.FieldPersonalInfoID)
		}
		if pitq.withLanguage != nil {
			_spec.Node.AddColumnOnce(personalinfotranslation.FieldLanguageCode)
		}
	}
	if ps := pitq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pitq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pitq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pitq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pitq *PersonalInfoTranslationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pitq.driver.Dialect())
	t1 := builder.Table(personalinfotranslation.Table)
	columns := pitq.ctx.Fields
	if len(columns) == 0 {
		columns = personalinfotranslation.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pitq.sql != nil {
		selector = pitq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pitq.ctx.Unique != nil && *pitq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range pitq.predicates {
		p(selector)
	}
	for _, p := range pitq.order {
		p(selector)
	}
	if offset := pitq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pitq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PersonalInfoTranslationGroupBy is the group-by builder for PersonalInfoTranslation entities.
type PersonalInfoTranslationGroupBy struct {
	selector
	build *PersonalInfoTranslationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pitgb *PersonalInfoTranslationGroupBy) Aggregate(fns ...AggregateFunc) *PersonalInfoTranslationGroupBy {
	pitgb.fns = append(pitgb.fns, fns...)
	return pitgb
}

// Scan applies the selector query and scans the result into the given value.
func (pitgb *PersonalInfoTranslationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pitgb.build.ctx, ent.OpQueryGroupBy)
	if err := pitgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PersonalInfoTranslationQuery, *PersonalInfoTranslationGroupBy](ctx, pitgb.build, pitgb, pitgb.build.inters, v)
}

func (pitgb *PersonalInfoTranslationGroupBy) sqlScan(ctx context.Context, root *PersonalInfoTranslationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pitgb.fns))
	for _, fn := range pitgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pitgb.flds)+len(pitgb.fns))
		for _, f := range *pitgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pitgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pitgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PersonalInfoTranslationSelect is the builder for selecting fields of PersonalInfoTranslation entities.
type PersonalInfoTranslationSelect struct {
	*PersonalInfoTranslationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pits *PersonalInfoTranslationSelect) Aggregate(fns ...AggregateFunc) *PersonalInfoTranslationSelect {
	pits.fns = append(pits.fns, fns...)
	return pits
}

// Scan applies the selector query and scans the result into the given value.
func (pits *PersonalInfoTranslationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pits.ctx, ent.OpQuerySelect)
	if err := pits.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PersonalInfoTranslationQuery, *PersonalInfoTranslationSelect](ctx, pits.PersonalInfoTranslationQuery, pits, pits.inters, v)
}

func (pits *PersonalInfoTranslationSelect) sqlScan(ctx context.Context, root *PersonalInfoTranslationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pits.fns))
	for _, fn := range pits.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pits.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pits.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
