// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"silan-backend/internal/ent/blogcategory"
	"silan-backend/internal/ent/blogcategorytranslation"
	"silan-backend/internal/ent/language"
	"silan-backend/internal/ent/predicate"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// BlogCategoryTranslationQuery is the builder for querying BlogCategoryTranslation entities.
type BlogCategoryTranslationQuery struct {
	config
	ctx              *QueryContext
	order            []blogcategorytranslation.OrderOption
	inters           []Interceptor
	predicates       []predicate.BlogCategoryTranslation
	withBlogCategory *BlogCategoryQuery
	withLanguage     *LanguageQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the BlogCategoryTranslationQuery builder.
func (bctq *BlogCategoryTranslationQuery) Where(ps ...predicate.BlogCategoryTranslation) *BlogCategoryTranslationQuery {
	bctq.predicates = append(bctq.predicates, ps...)
	return bctq
}

// Limit the number of records to be returned by this query.
func (bctq *BlogCategoryTranslationQuery) Limit(limit int) *BlogCategoryTranslationQuery {
	bctq.ctx.Limit = &limit
	return bctq
}

// Offset to start from.
func (bctq *BlogCategoryTranslationQuery) Offset(offset int) *BlogCategoryTranslationQuery {
	bctq.ctx.Offset = &offset
	return bctq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (bctq *BlogCategoryTranslationQuery) Unique(unique bool) *BlogCategoryTranslationQuery {
	bctq.ctx.Unique = &unique
	return bctq
}

// Order specifies how the records should be ordered.
func (bctq *BlogCategoryTranslationQuery) Order(o ...blogcategorytranslation.OrderOption) *BlogCategoryTranslationQuery {
	bctq.order = append(bctq.order, o...)
	return bctq
}

// QueryBlogCategory chains the current query on the "blog_category" edge.
func (bctq *BlogCategoryTranslationQuery) QueryBlogCategory() *BlogCategoryQuery {
	query := (&BlogCategoryClient{config: bctq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := bctq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := bctq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(blogcategorytranslation.Table, blogcategorytranslation.FieldID, selector),
			sqlgraph.To(blogcategory.Table, blogcategory.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, blogcategorytranslation.BlogCategoryTable, blogcategorytranslation.BlogCategoryColumn),
		)
		fromU = sqlgraph.SetNeighbors(bctq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryLanguage chains the current query on the "language" edge.
func (bctq *BlogCategoryTranslationQuery) QueryLanguage() *LanguageQuery {
	query := (&LanguageClient{config: bctq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := bctq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := bctq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(blogcategorytranslation.Table, blogcategorytranslation.FieldID, selector),
			sqlgraph.To(language.Table, language.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, blogcategorytranslation.LanguageTable, blogcategorytranslation.LanguageColumn),
		)
		fromU = sqlgraph.SetNeighbors(bctq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first BlogCategoryTranslation entity from the query.
// Returns a *NotFoundError when no BlogCategoryTranslation was found.
func (bctq *BlogCategoryTranslationQuery) First(ctx context.Context) (*BlogCategoryTranslation, error) {
	nodes, err := bctq.Limit(1).All(setContextOp(ctx, bctq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{blogcategorytranslation.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (bctq *BlogCategoryTranslationQuery) FirstX(ctx context.Context) *BlogCategoryTranslation {
	node, err := bctq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first BlogCategoryTranslation ID from the query.
// Returns a *NotFoundError when no BlogCategoryTranslation ID was found.
func (bctq *BlogCategoryTranslationQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = bctq.Limit(1).IDs(setContextOp(ctx, bctq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{blogcategorytranslation.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (bctq *BlogCategoryTranslationQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := bctq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single BlogCategoryTranslation entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one BlogCategoryTranslation entity is found.
// Returns a *NotFoundError when no BlogCategoryTranslation entities are found.
func (bctq *BlogCategoryTranslationQuery) Only(ctx context.Context) (*BlogCategoryTranslation, error) {
	nodes, err := bctq.Limit(2).All(setContextOp(ctx, bctq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{blogcategorytranslation.Label}
	default:
		return nil, &NotSingularError{blogcategorytranslation.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (bctq *BlogCategoryTranslationQuery) OnlyX(ctx context.Context) *BlogCategoryTranslation {
	node, err := bctq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only BlogCategoryTranslation ID in the query.
// Returns a *NotSingularError when more than one BlogCategoryTranslation ID is found.
// Returns a *NotFoundError when no entities are found.
func (bctq *BlogCategoryTranslationQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = bctq.Limit(2).IDs(setContextOp(ctx, bctq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{blogcategorytranslation.Label}
	default:
		err = &NotSingularError{blogcategorytranslation.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (bctq *BlogCategoryTranslationQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := bctq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of BlogCategoryTranslations.
func (bctq *BlogCategoryTranslationQuery) All(ctx context.Context) ([]*BlogCategoryTranslation, error) {
	ctx = setContextOp(ctx, bctq.ctx, ent.OpQueryAll)
	if err := bctq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*BlogCategoryTranslation, *BlogCategoryTranslationQuery]()
	return withInterceptors[[]*BlogCategoryTranslation](ctx, bctq, qr, bctq.inters)
}

// AllX is like All, but panics if an error occurs.
func (bctq *BlogCategoryTranslationQuery) AllX(ctx context.Context) []*BlogCategoryTranslation {
	nodes, err := bctq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of BlogCategoryTranslation IDs.
func (bctq *BlogCategoryTranslationQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if bctq.ctx.Unique == nil && bctq.path != nil {
		bctq.Unique(true)
	}
	ctx = setContextOp(ctx, bctq.ctx, ent.OpQueryIDs)
	if err = bctq.Select(blogcategorytranslation.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (bctq *BlogCategoryTranslationQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := bctq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (bctq *BlogCategoryTranslationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, bctq.ctx, ent.OpQueryCount)
	if err := bctq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, bctq, querierCount[*BlogCategoryTranslationQuery](), bctq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (bctq *BlogCategoryTranslationQuery) CountX(ctx context.Context) int {
	count, err := bctq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (bctq *BlogCategoryTranslationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, bctq.ctx, ent.OpQueryExist)
	switch _, err := bctq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (bctq *BlogCategoryTranslationQuery) ExistX(ctx context.Context) bool {
	exist, err := bctq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the BlogCategoryTranslationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (bctq *BlogCategoryTranslationQuery) Clone() *BlogCategoryTranslationQuery {
	if bctq == nil {
		return nil
	}
	return &BlogCategoryTranslationQuery{
		config:           bctq.config,
		ctx:              bctq.ctx.Clone(),
		order:            append([]blogcategorytranslation.OrderOption{}, bctq.order...),
		inters:           append([]Interceptor{}, bctq.inters...),
		predicates:       append([]predicate.BlogCategoryTranslation{}, bctq.predicates...),
		withBlogCategory: bctq.withBlogCategory.Clone(),
		withLanguage:     bctq.withLanguage.Clone(),
		// clone intermediate query.
		sql:  bctq.sql.Clone(),
		path: bctq.path,
	}
}

// WithBlogCategory tells the query-builder to eager-load the nodes that are connected to
// the "blog_category" edge. The optional arguments are used to configure the query builder of the edge.
func (bctq *BlogCategoryTranslationQuery) WithBlogCategory(opts ...func(*BlogCategoryQuery)) *BlogCategoryTranslationQuery {
	query := (&BlogCategoryClient{config: bctq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	bctq.withBlogCategory = query
	return bctq
}

// WithLanguage tells the query-builder to eager-load the nodes that are connected to
// the "language" edge. The optional arguments are used to configure the query builder of the edge.
func (bctq *BlogCategoryTranslationQuery) WithLanguage(opts ...func(*LanguageQuery)) *BlogCategoryTranslationQuery {
	query := (&LanguageClient{config: bctq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	bctq.withLanguage = query
	return bctq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		BlogCategoryID uuid.UUID `json:"blog_category_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.BlogCategoryTranslation.Query().
//		GroupBy(blogcategorytranslation.FieldBlogCategoryID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (bctq *BlogCategoryTranslationQuery) GroupBy(field string, fields ...string) *BlogCategoryTranslationGroupBy {
	bctq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &BlogCategoryTranslationGroupBy{build: bctq}
	grbuild.flds = &bctq.ctx.Fields
	grbuild.label = blogcategorytranslation.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		BlogCategoryID uuid.UUID `json:"blog_category_id,omitempty"`
//	}
//
//	client.BlogCategoryTranslation.Query().
//		Select(blogcategorytranslation.FieldBlogCategoryID).
//		Scan(ctx, &v)
func (bctq *BlogCategoryTranslationQuery) Select(fields ...string) *BlogCategoryTranslationSelect {
	bctq.ctx.Fields = append(bctq.ctx.Fields, fields...)
	sbuild := &BlogCategoryTranslationSelect{BlogCategoryTranslationQuery: bctq}
	sbuild.label = blogcategorytranslation.Label
	sbuild.flds, sbuild.scan = &bctq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a BlogCategoryTranslationSelect configured with the given aggregations.
func (bctq *BlogCategoryTranslationQuery) Aggregate(fns ...AggregateFunc) *BlogCategoryTranslationSelect {
	return bctq.Select().Aggregate(fns...)
}

func (bctq *BlogCategoryTranslationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range bctq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, bctq); err != nil {
				return err
			}
		}
	}
	for _, f := range bctq.ctx.Fields {
		if !blogcategorytranslation.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if bctq.path != nil {
		prev, err := bctq.path(ctx)
		if err != nil {
			return err
		}
		bctq.sql = prev
	}
	return nil
}

func (bctq *BlogCategoryTranslationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*BlogCategoryTranslation, error) {
	var (
		nodes       = []*BlogCategoryTranslation{}
		_spec       = bctq.querySpec()
		loadedTypes = [2]bool{
			bctq.withBlogCategory != nil,
			bctq.withLanguage != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*BlogCategoryTranslation).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &BlogCategoryTranslation{config: bctq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, bctq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := bctq.withBlogCategory; query != nil {
		if err := bctq.loadBlogCategory(ctx, query, nodes, nil,
			func(n *BlogCategoryTranslation, e *BlogCategory) { n.Edges.BlogCategory = e }); err != nil {
			return nil, err
		}
	}
	if query := bctq.withLanguage; query != nil {
		if err := bctq.loadLanguage(ctx, query, nodes, nil,
			func(n *BlogCategoryTranslation, e *Language) { n.Edges.Language = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (bctq *BlogCategoryTranslationQuery) loadBlogCategory(ctx context.Context, query *BlogCategoryQuery, nodes []*BlogCategoryTranslation, init func(*BlogCategoryTranslation), assign func(*BlogCategoryTranslation, *BlogCategory)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*BlogCategoryTranslation)
	for i := range nodes {
		fk := nodes[i].BlogCategoryID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(blogcategory.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "blog_category_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (bctq *BlogCategoryTranslationQuery) loadLanguage(ctx context.Context, query *LanguageQuery, nodes []*BlogCategoryTranslation, init func(*BlogCategoryTranslation), assign func(*BlogCategoryTranslation, *Language)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*BlogCategoryTranslation)
	for i := range nodes {
		fk := nodes[i].LanguageCode
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(language.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "language_code" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (bctq *BlogCategoryTranslationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := bctq.querySpec()
	_spec.Node.Columns = bctq.ctx.Fields
	if len(bctq.ctx.Fields) > 0 {
		_spec.Unique = bctq.ctx.Unique != nil && *bctq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, bctq.driver, _spec)
}

func (bctq *BlogCategoryTranslationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(blogcategorytranslation.Table, blogcategorytranslation.Columns, sqlgraph.NewFieldSpec(blogcategorytranslation.FieldID, field.TypeUUID))
	_spec.From = bctq.sql
	if unique := bctq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if bctq.path != nil {
		_spec.Unique = true
	}
	if fields := bctq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, blogcategorytranslation.FieldID)
		for i := range fields {
			if fields[i] != blogcategorytranslation.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if bctq.withBlogCategory != nil {
			_spec.Node.AddColumnOnce(blogcategorytranslation.FieldBlogCategoryID)
		}
		if bctq.withLanguage != nil {
			_spec.Node.AddColumnOnce(blogcategorytranslation.FieldLanguageCode)
		}
	}
	if ps := bctq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := bctq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := bctq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := bctq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (bctq *BlogCategoryTranslationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(bctq.driver.Dialect())
	t1 := builder.Table(blogcategorytranslation.Table)
	columns := bctq.ctx.Fields
	if len(columns) == 0 {
		columns = blogcategorytranslation.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if bctq.sql != nil {
		selector = bctq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if bctq.ctx.Unique != nil && *bctq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range bctq.predicates {
		p(selector)
	}
	for _, p := range bctq.order {
		p(selector)
	}
	if offset := bctq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := bctq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// BlogCategoryTranslationGroupBy is the group-by builder for BlogCategoryTranslation entities.
type BlogCategoryTranslationGroupBy struct {
	selector
	build *BlogCategoryTranslationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (bctgb *BlogCategoryTranslationGroupBy) Aggregate(fns ...AggregateFunc) *BlogCategoryTranslationGroupBy {
	bctgb.fns = append(bctgb.fns, fns...)
	return bctgb
}

// Scan applies the selector query and scans the result into the given value.
func (bctgb *BlogCategoryTranslationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, bctgb.build.ctx, ent.OpQueryGroupBy)
	if err := bctgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BlogCategoryTranslationQuery, *BlogCategoryTranslationGroupBy](ctx, bctgb.build, bctgb, bctgb.build.inters, v)
}

func (bctgb *BlogCategoryTranslationGroupBy) sqlScan(ctx context.Context, root *BlogCategoryTranslationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(bctgb.fns))
	for _, fn := range bctgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*bctgb.flds)+len(bctgb.fns))
		for _, f := range *bctgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*bctgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := bctgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// BlogCategoryTranslationSelect is the builder for selecting fields of BlogCategoryTranslation entities.
type BlogCategoryTranslationSelect struct {
	*BlogCategoryTranslationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (bcts *BlogCategoryTranslationSelect) Aggregate(fns ...AggregateFunc) *BlogCategoryTranslationSelect {
	bcts.fns = append(bcts.fns, fns...)
	return bcts
}

// Scan applies the selector query and scans the result into the given value.
func (bcts *BlogCategoryTranslationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, bcts.ctx, ent.OpQuerySelect)
	if err := bcts.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BlogCategoryTranslationQuery, *BlogCategoryTranslationSelect](ctx, bcts.BlogCategoryTranslationQuery, bcts, bcts.inters, v)
}

func (bcts *BlogCategoryTranslationSelect) sqlScan(ctx context.Context, root *BlogCategoryTranslationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(bcts.fns))
	for _, fn := range bcts.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*bcts.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := bcts.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
