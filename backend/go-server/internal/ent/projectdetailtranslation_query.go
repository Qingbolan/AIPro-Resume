// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"silan-backend/internal/ent/language"
	"silan-backend/internal/ent/predicate"
	"silan-backend/internal/ent/projectdetail"
	"silan-backend/internal/ent/projectdetailtranslation"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// ProjectDetailTranslationQuery is the builder for querying ProjectDetailTranslation entities.
type ProjectDetailTranslationQuery struct {
	config
	ctx               *QueryContext
	order             []projectdetailtranslation.OrderOption
	inters            []Interceptor
	predicates        []predicate.ProjectDetailTranslation
	withProjectDetail *ProjectDetailQuery
	withLanguage      *LanguageQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ProjectDetailTranslationQuery builder.
func (pdtq *ProjectDetailTranslationQuery) Where(ps ...predicate.ProjectDetailTranslation) *ProjectDetailTranslationQuery {
	pdtq.predicates = append(pdtq.predicates, ps...)
	return pdtq
}

// Limit the number of records to be returned by this query.
func (pdtq *ProjectDetailTranslationQuery) Limit(limit int) *ProjectDetailTranslationQuery {
	pdtq.ctx.Limit = &limit
	return pdtq
}

// Offset to start from.
func (pdtq *ProjectDetailTranslationQuery) Offset(offset int) *ProjectDetailTranslationQuery {
	pdtq.ctx.Offset = &offset
	return pdtq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pdtq *ProjectDetailTranslationQuery) Unique(unique bool) *ProjectDetailTranslationQuery {
	pdtq.ctx.Unique = &unique
	return pdtq
}

// Order specifies how the records should be ordered.
func (pdtq *ProjectDetailTranslationQuery) Order(o ...projectdetailtranslation.OrderOption) *ProjectDetailTranslationQuery {
	pdtq.order = append(pdtq.order, o...)
	return pdtq
}

// QueryProjectDetail chains the current query on the "project_detail" edge.
func (pdtq *ProjectDetailTranslationQuery) QueryProjectDetail() *ProjectDetailQuery {
	query := (&ProjectDetailClient{config: pdtq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pdtq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pdtq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(projectdetailtranslation.Table, projectdetailtranslation.FieldID, selector),
			sqlgraph.To(projectdetail.Table, projectdetail.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, projectdetailtranslation.ProjectDetailTable, projectdetailtranslation.ProjectDetailColumn),
		)
		fromU = sqlgraph.SetNeighbors(pdtq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryLanguage chains the current query on the "language" edge.
func (pdtq *ProjectDetailTranslationQuery) QueryLanguage() *LanguageQuery {
	query := (&LanguageClient{config: pdtq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pdtq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pdtq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(projectdetailtranslation.Table, projectdetailtranslation.FieldID, selector),
			sqlgraph.To(language.Table, language.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, projectdetailtranslation.LanguageTable, projectdetailtranslation.LanguageColumn),
		)
		fromU = sqlgraph.SetNeighbors(pdtq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ProjectDetailTranslation entity from the query.
// Returns a *NotFoundError when no ProjectDetailTranslation was found.
func (pdtq *ProjectDetailTranslationQuery) First(ctx context.Context) (*ProjectDetailTranslation, error) {
	nodes, err := pdtq.Limit(1).All(setContextOp(ctx, pdtq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{projectdetailtranslation.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pdtq *ProjectDetailTranslationQuery) FirstX(ctx context.Context) *ProjectDetailTranslation {
	node, err := pdtq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ProjectDetailTranslation ID from the query.
// Returns a *NotFoundError when no ProjectDetailTranslation ID was found.
func (pdtq *ProjectDetailTranslationQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = pdtq.Limit(1).IDs(setContextOp(ctx, pdtq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{projectdetailtranslation.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pdtq *ProjectDetailTranslationQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := pdtq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ProjectDetailTranslation entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ProjectDetailTranslation entity is found.
// Returns a *NotFoundError when no ProjectDetailTranslation entities are found.
func (pdtq *ProjectDetailTranslationQuery) Only(ctx context.Context) (*ProjectDetailTranslation, error) {
	nodes, err := pdtq.Limit(2).All(setContextOp(ctx, pdtq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{projectdetailtranslation.Label}
	default:
		return nil, &NotSingularError{projectdetailtranslation.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pdtq *ProjectDetailTranslationQuery) OnlyX(ctx context.Context) *ProjectDetailTranslation {
	node, err := pdtq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ProjectDetailTranslation ID in the query.
// Returns a *NotSingularError when more than one ProjectDetailTranslation ID is found.
// Returns a *NotFoundError when no entities are found.
func (pdtq *ProjectDetailTranslationQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = pdtq.Limit(2).IDs(setContextOp(ctx, pdtq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{projectdetailtranslation.Label}
	default:
		err = &NotSingularError{projectdetailtranslation.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pdtq *ProjectDetailTranslationQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := pdtq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ProjectDetailTranslations.
func (pdtq *ProjectDetailTranslationQuery) All(ctx context.Context) ([]*ProjectDetailTranslation, error) {
	ctx = setContextOp(ctx, pdtq.ctx, ent.OpQueryAll)
	if err := pdtq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ProjectDetailTranslation, *ProjectDetailTranslationQuery]()
	return withInterceptors[[]*ProjectDetailTranslation](ctx, pdtq, qr, pdtq.inters)
}

// AllX is like All, but panics if an error occurs.
func (pdtq *ProjectDetailTranslationQuery) AllX(ctx context.Context) []*ProjectDetailTranslation {
	nodes, err := pdtq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ProjectDetailTranslation IDs.
func (pdtq *ProjectDetailTranslationQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if pdtq.ctx.Unique == nil && pdtq.path != nil {
		pdtq.Unique(true)
	}
	ctx = setContextOp(ctx, pdtq.ctx, ent.OpQueryIDs)
	if err = pdtq.Select(projectdetailtranslation.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pdtq *ProjectDetailTranslationQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := pdtq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pdtq *ProjectDetailTranslationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, pdtq.ctx, ent.OpQueryCount)
	if err := pdtq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, pdtq, querierCount[*ProjectDetailTranslationQuery](), pdtq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (pdtq *ProjectDetailTranslationQuery) CountX(ctx context.Context) int {
	count, err := pdtq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pdtq *ProjectDetailTranslationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, pdtq.ctx, ent.OpQueryExist)
	switch _, err := pdtq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (pdtq *ProjectDetailTranslationQuery) ExistX(ctx context.Context) bool {
	exist, err := pdtq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ProjectDetailTranslationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pdtq *ProjectDetailTranslationQuery) Clone() *ProjectDetailTranslationQuery {
	if pdtq == nil {
		return nil
	}
	return &ProjectDetailTranslationQuery{
		config:            pdtq.config,
		ctx:               pdtq.ctx.Clone(),
		order:             append([]projectdetailtranslation.OrderOption{}, pdtq.order...),
		inters:            append([]Interceptor{}, pdtq.inters...),
		predicates:        append([]predicate.ProjectDetailTranslation{}, pdtq.predicates...),
		withProjectDetail: pdtq.withProjectDetail.Clone(),
		withLanguage:      pdtq.withLanguage.Clone(),
		// clone intermediate query.
		sql:  pdtq.sql.Clone(),
		path: pdtq.path,
	}
}

// WithProjectDetail tells the query-builder to eager-load the nodes that are connected to
// the "project_detail" edge. The optional arguments are used to configure the query builder of the edge.
func (pdtq *ProjectDetailTranslationQuery) WithProjectDetail(opts ...func(*ProjectDetailQuery)) *ProjectDetailTranslationQuery {
	query := (&ProjectDetailClient{config: pdtq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pdtq.withProjectDetail = query
	return pdtq
}

// WithLanguage tells the query-builder to eager-load the nodes that are connected to
// the "language" edge. The optional arguments are used to configure the query builder of the edge.
func (pdtq *ProjectDetailTranslationQuery) WithLanguage(opts ...func(*LanguageQuery)) *ProjectDetailTranslationQuery {
	query := (&LanguageClient{config: pdtq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pdtq.withLanguage = query
	return pdtq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ProjectDetailID uuid.UUID `json:"project_detail_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ProjectDetailTranslation.Query().
//		GroupBy(projectdetailtranslation.FieldProjectDetailID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (pdtq *ProjectDetailTranslationQuery) GroupBy(field string, fields ...string) *ProjectDetailTranslationGroupBy {
	pdtq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ProjectDetailTranslationGroupBy{build: pdtq}
	grbuild.flds = &pdtq.ctx.Fields
	grbuild.label = projectdetailtranslation.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ProjectDetailID uuid.UUID `json:"project_detail_id,omitempty"`
//	}
//
//	client.ProjectDetailTranslation.Query().
//		Select(projectdetailtranslation.FieldProjectDetailID).
//		Scan(ctx, &v)
func (pdtq *ProjectDetailTranslationQuery) Select(fields ...string) *ProjectDetailTranslationSelect {
	pdtq.ctx.Fields = append(pdtq.ctx.Fields, fields...)
	sbuild := &ProjectDetailTranslationSelect{ProjectDetailTranslationQuery: pdtq}
	sbuild.label = projectdetailtranslation.Label
	sbuild.flds, sbuild.scan = &pdtq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ProjectDetailTranslationSelect configured with the given aggregations.
func (pdtq *ProjectDetailTranslationQuery) Aggregate(fns ...AggregateFunc) *ProjectDetailTranslationSelect {
	return pdtq.Select().Aggregate(fns...)
}

func (pdtq *ProjectDetailTranslationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range pdtq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, pdtq); err != nil {
				return err
			}
		}
	}
	for _, f := range pdtq.ctx.Fields {
		if !projectdetailtranslation.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pdtq.path != nil {
		prev, err := pdtq.path(ctx)
		if err != nil {
			return err
		}
		pdtq.sql = prev
	}
	return nil
}

func (pdtq *ProjectDetailTranslationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ProjectDetailTranslation, error) {
	var (
		nodes       = []*ProjectDetailTranslation{}
		_spec       = pdtq.querySpec()
		loadedTypes = [2]bool{
			pdtq.withProjectDetail != nil,
			pdtq.withLanguage != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ProjectDetailTranslation).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ProjectDetailTranslation{config: pdtq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pdtq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := pdtq.withProjectDetail; query != nil {
		if err := pdtq.loadProjectDetail(ctx, query, nodes, nil,
			func(n *ProjectDetailTranslation, e *ProjectDetail) { n.Edges.ProjectDetail = e }); err != nil {
			return nil, err
		}
	}
	if query := pdtq.withLanguage; query != nil {
		if err := pdtq.loadLanguage(ctx, query, nodes, nil,
			func(n *ProjectDetailTranslation, e *Language) { n.Edges.Language = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (pdtq *ProjectDetailTranslationQuery) loadProjectDetail(ctx context.Context, query *ProjectDetailQuery, nodes []*ProjectDetailTranslation, init func(*ProjectDetailTranslation), assign func(*ProjectDetailTranslation, *ProjectDetail)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ProjectDetailTranslation)
	for i := range nodes {
		fk := nodes[i].ProjectDetailID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(projectdetail.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "project_detail_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (pdtq *ProjectDetailTranslationQuery) loadLanguage(ctx context.Context, query *LanguageQuery, nodes []*ProjectDetailTranslation, init func(*ProjectDetailTranslation), assign func(*ProjectDetailTranslation, *Language)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*ProjectDetailTranslation)
	for i := range nodes {
		fk := nodes[i].LanguageCode
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(language.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "language_code" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (pdtq *ProjectDetailTranslationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pdtq.querySpec()
	_spec.Node.Columns = pdtq.ctx.Fields
	if len(pdtq.ctx.Fields) > 0 {
		_spec.Unique = pdtq.ctx.Unique != nil && *pdtq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, pdtq.driver, _spec)
}

func (pdtq *ProjectDetailTranslationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(projectdetailtranslation.Table, projectdetailtranslation.Columns, sqlgraph.NewFieldSpec(projectdetailtranslation.FieldID, field.TypeUUID))
	_spec.From = pdtq.sql
	if unique := pdtq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if pdtq.path != nil {
		_spec.Unique = true
	}
	if fields := pdtq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, projectdetailtranslation.FieldID)
		for i := range fields {
			if fields[i] != projectdetailtranslation.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if pdtq.withProjectDetail != nil {
			_spec.Node.AddColumnOnce(projectdetailtranslation.FieldProjectDetailID)
		}
		if pdtq.withLanguage != nil {
			_spec.Node.AddColumnOnce(projectdetailtranslation.FieldLanguageCode)
		}
	}
	if ps := pdtq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pdtq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pdtq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pdtq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pdtq *ProjectDetailTranslationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pdtq.driver.Dialect())
	t1 := builder.Table(projectdetailtranslation.Table)
	columns := pdtq.ctx.Fields
	if len(columns) == 0 {
		columns = projectdetailtranslation.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pdtq.sql != nil {
		selector = pdtq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pdtq.ctx.Unique != nil && *pdtq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range pdtq.predicates {
		p(selector)
	}
	for _, p := range pdtq.order {
		p(selector)
	}
	if offset := pdtq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pdtq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ProjectDetailTranslationGroupBy is the group-by builder for ProjectDetailTranslation entities.
type ProjectDetailTranslationGroupBy struct {
	selector
	build *ProjectDetailTranslationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pdtgb *ProjectDetailTranslationGroupBy) Aggregate(fns ...AggregateFunc) *ProjectDetailTranslationGroupBy {
	pdtgb.fns = append(pdtgb.fns, fns...)
	return pdtgb
}

// Scan applies the selector query and scans the result into the given value.
func (pdtgb *ProjectDetailTranslationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pdtgb.build.ctx, ent.OpQueryGroupBy)
	if err := pdtgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProjectDetailTranslationQuery, *ProjectDetailTranslationGroupBy](ctx, pdtgb.build, pdtgb, pdtgb.build.inters, v)
}

func (pdtgb *ProjectDetailTranslationGroupBy) sqlScan(ctx context.Context, root *ProjectDetailTranslationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pdtgb.fns))
	for _, fn := range pdtgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pdtgb.flds)+len(pdtgb.fns))
		for _, f := range *pdtgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pdtgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pdtgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ProjectDetailTranslationSelect is the builder for selecting fields of ProjectDetailTranslation entities.
type ProjectDetailTranslationSelect struct {
	*ProjectDetailTranslationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pdts *ProjectDetailTranslationSelect) Aggregate(fns ...AggregateFunc) *ProjectDetailTranslationSelect {
	pdts.fns = append(pdts.fns, fns...)
	return pdts
}

// Scan applies the selector query and scans the result into the given value.
func (pdts *ProjectDetailTranslationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pdts.ctx, ent.OpQuerySelect)
	if err := pdts.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProjectDetailTranslationQuery, *ProjectDetailTranslationSelect](ctx, pdts.ProjectDetailTranslationQuery, pdts, pdts.inters, v)
}

func (pdts *ProjectDetailTranslationSelect) sqlScan(ctx context.Context, root *ProjectDetailTranslationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pdts.fns))
	for _, fn := range pdts.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pdts.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pdts.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
