// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"silan-backend/internal/ent/predicate"
	"silan-backend/internal/ent/researchproject"
	"silan-backend/internal/ent/researchprojectdetail"
	"silan-backend/internal/ent/researchprojecttranslation"
	"silan-backend/internal/ent/user"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// ResearchProjectQuery is the builder for querying ResearchProject entities.
type ResearchProjectQuery struct {
	config
	ctx              *QueryContext
	order            []researchproject.OrderOption
	inters           []Interceptor
	predicates       []predicate.ResearchProject
	withUser         *UserQuery
	withTranslations *ResearchProjectTranslationQuery
	withDetails      *ResearchProjectDetailQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ResearchProjectQuery builder.
func (rpq *ResearchProjectQuery) Where(ps ...predicate.ResearchProject) *ResearchProjectQuery {
	rpq.predicates = append(rpq.predicates, ps...)
	return rpq
}

// Limit the number of records to be returned by this query.
func (rpq *ResearchProjectQuery) Limit(limit int) *ResearchProjectQuery {
	rpq.ctx.Limit = &limit
	return rpq
}

// Offset to start from.
func (rpq *ResearchProjectQuery) Offset(offset int) *ResearchProjectQuery {
	rpq.ctx.Offset = &offset
	return rpq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (rpq *ResearchProjectQuery) Unique(unique bool) *ResearchProjectQuery {
	rpq.ctx.Unique = &unique
	return rpq
}

// Order specifies how the records should be ordered.
func (rpq *ResearchProjectQuery) Order(o ...researchproject.OrderOption) *ResearchProjectQuery {
	rpq.order = append(rpq.order, o...)
	return rpq
}

// QueryUser chains the current query on the "user" edge.
func (rpq *ResearchProjectQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: rpq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rpq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rpq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(researchproject.Table, researchproject.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, researchproject.UserTable, researchproject.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(rpq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTranslations chains the current query on the "translations" edge.
func (rpq *ResearchProjectQuery) QueryTranslations() *ResearchProjectTranslationQuery {
	query := (&ResearchProjectTranslationClient{config: rpq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rpq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rpq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(researchproject.Table, researchproject.FieldID, selector),
			sqlgraph.To(researchprojecttranslation.Table, researchprojecttranslation.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, researchproject.TranslationsTable, researchproject.TranslationsColumn),
		)
		fromU = sqlgraph.SetNeighbors(rpq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryDetails chains the current query on the "details" edge.
func (rpq *ResearchProjectQuery) QueryDetails() *ResearchProjectDetailQuery {
	query := (&ResearchProjectDetailClient{config: rpq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rpq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rpq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(researchproject.Table, researchproject.FieldID, selector),
			sqlgraph.To(researchprojectdetail.Table, researchprojectdetail.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, researchproject.DetailsTable, researchproject.DetailsColumn),
		)
		fromU = sqlgraph.SetNeighbors(rpq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ResearchProject entity from the query.
// Returns a *NotFoundError when no ResearchProject was found.
func (rpq *ResearchProjectQuery) First(ctx context.Context) (*ResearchProject, error) {
	nodes, err := rpq.Limit(1).All(setContextOp(ctx, rpq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{researchproject.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (rpq *ResearchProjectQuery) FirstX(ctx context.Context) *ResearchProject {
	node, err := rpq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ResearchProject ID from the query.
// Returns a *NotFoundError when no ResearchProject ID was found.
func (rpq *ResearchProjectQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = rpq.Limit(1).IDs(setContextOp(ctx, rpq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{researchproject.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (rpq *ResearchProjectQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := rpq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ResearchProject entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ResearchProject entity is found.
// Returns a *NotFoundError when no ResearchProject entities are found.
func (rpq *ResearchProjectQuery) Only(ctx context.Context) (*ResearchProject, error) {
	nodes, err := rpq.Limit(2).All(setContextOp(ctx, rpq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{researchproject.Label}
	default:
		return nil, &NotSingularError{researchproject.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (rpq *ResearchProjectQuery) OnlyX(ctx context.Context) *ResearchProject {
	node, err := rpq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ResearchProject ID in the query.
// Returns a *NotSingularError when more than one ResearchProject ID is found.
// Returns a *NotFoundError when no entities are found.
func (rpq *ResearchProjectQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = rpq.Limit(2).IDs(setContextOp(ctx, rpq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{researchproject.Label}
	default:
		err = &NotSingularError{researchproject.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (rpq *ResearchProjectQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := rpq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ResearchProjects.
func (rpq *ResearchProjectQuery) All(ctx context.Context) ([]*ResearchProject, error) {
	ctx = setContextOp(ctx, rpq.ctx, ent.OpQueryAll)
	if err := rpq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ResearchProject, *ResearchProjectQuery]()
	return withInterceptors[[]*ResearchProject](ctx, rpq, qr, rpq.inters)
}

// AllX is like All, but panics if an error occurs.
func (rpq *ResearchProjectQuery) AllX(ctx context.Context) []*ResearchProject {
	nodes, err := rpq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ResearchProject IDs.
func (rpq *ResearchProjectQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if rpq.ctx.Unique == nil && rpq.path != nil {
		rpq.Unique(true)
	}
	ctx = setContextOp(ctx, rpq.ctx, ent.OpQueryIDs)
	if err = rpq.Select(researchproject.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (rpq *ResearchProjectQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := rpq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (rpq *ResearchProjectQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, rpq.ctx, ent.OpQueryCount)
	if err := rpq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, rpq, querierCount[*ResearchProjectQuery](), rpq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (rpq *ResearchProjectQuery) CountX(ctx context.Context) int {
	count, err := rpq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (rpq *ResearchProjectQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, rpq.ctx, ent.OpQueryExist)
	switch _, err := rpq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (rpq *ResearchProjectQuery) ExistX(ctx context.Context) bool {
	exist, err := rpq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ResearchProjectQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (rpq *ResearchProjectQuery) Clone() *ResearchProjectQuery {
	if rpq == nil {
		return nil
	}
	return &ResearchProjectQuery{
		config:           rpq.config,
		ctx:              rpq.ctx.Clone(),
		order:            append([]researchproject.OrderOption{}, rpq.order...),
		inters:           append([]Interceptor{}, rpq.inters...),
		predicates:       append([]predicate.ResearchProject{}, rpq.predicates...),
		withUser:         rpq.withUser.Clone(),
		withTranslations: rpq.withTranslations.Clone(),
		withDetails:      rpq.withDetails.Clone(),
		// clone intermediate query.
		sql:  rpq.sql.Clone(),
		path: rpq.path,
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (rpq *ResearchProjectQuery) WithUser(opts ...func(*UserQuery)) *ResearchProjectQuery {
	query := (&UserClient{config: rpq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rpq.withUser = query
	return rpq
}

// WithTranslations tells the query-builder to eager-load the nodes that are connected to
// the "translations" edge. The optional arguments are used to configure the query builder of the edge.
func (rpq *ResearchProjectQuery) WithTranslations(opts ...func(*ResearchProjectTranslationQuery)) *ResearchProjectQuery {
	query := (&ResearchProjectTranslationClient{config: rpq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rpq.withTranslations = query
	return rpq
}

// WithDetails tells the query-builder to eager-load the nodes that are connected to
// the "details" edge. The optional arguments are used to configure the query builder of the edge.
func (rpq *ResearchProjectQuery) WithDetails(opts ...func(*ResearchProjectDetailQuery)) *ResearchProjectQuery {
	query := (&ResearchProjectDetailClient{config: rpq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rpq.withDetails = query
	return rpq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserID uuid.UUID `json:"user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ResearchProject.Query().
//		GroupBy(researchproject.FieldUserID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (rpq *ResearchProjectQuery) GroupBy(field string, fields ...string) *ResearchProjectGroupBy {
	rpq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ResearchProjectGroupBy{build: rpq}
	grbuild.flds = &rpq.ctx.Fields
	grbuild.label = researchproject.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserID uuid.UUID `json:"user_id,omitempty"`
//	}
//
//	client.ResearchProject.Query().
//		Select(researchproject.FieldUserID).
//		Scan(ctx, &v)
func (rpq *ResearchProjectQuery) Select(fields ...string) *ResearchProjectSelect {
	rpq.ctx.Fields = append(rpq.ctx.Fields, fields...)
	sbuild := &ResearchProjectSelect{ResearchProjectQuery: rpq}
	sbuild.label = researchproject.Label
	sbuild.flds, sbuild.scan = &rpq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ResearchProjectSelect configured with the given aggregations.
func (rpq *ResearchProjectQuery) Aggregate(fns ...AggregateFunc) *ResearchProjectSelect {
	return rpq.Select().Aggregate(fns...)
}

func (rpq *ResearchProjectQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range rpq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, rpq); err != nil {
				return err
			}
		}
	}
	for _, f := range rpq.ctx.Fields {
		if !researchproject.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if rpq.path != nil {
		prev, err := rpq.path(ctx)
		if err != nil {
			return err
		}
		rpq.sql = prev
	}
	return nil
}

func (rpq *ResearchProjectQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ResearchProject, error) {
	var (
		nodes       = []*ResearchProject{}
		_spec       = rpq.querySpec()
		loadedTypes = [3]bool{
			rpq.withUser != nil,
			rpq.withTranslations != nil,
			rpq.withDetails != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ResearchProject).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ResearchProject{config: rpq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, rpq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := rpq.withUser; query != nil {
		if err := rpq.loadUser(ctx, query, nodes, nil,
			func(n *ResearchProject, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	if query := rpq.withTranslations; query != nil {
		if err := rpq.loadTranslations(ctx, query, nodes,
			func(n *ResearchProject) { n.Edges.Translations = []*ResearchProjectTranslation{} },
			func(n *ResearchProject, e *ResearchProjectTranslation) {
				n.Edges.Translations = append(n.Edges.Translations, e)
			}); err != nil {
			return nil, err
		}
	}
	if query := rpq.withDetails; query != nil {
		if err := rpq.loadDetails(ctx, query, nodes,
			func(n *ResearchProject) { n.Edges.Details = []*ResearchProjectDetail{} },
			func(n *ResearchProject, e *ResearchProjectDetail) { n.Edges.Details = append(n.Edges.Details, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (rpq *ResearchProjectQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*ResearchProject, init func(*ResearchProject), assign func(*ResearchProject, *User)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ResearchProject)
	for i := range nodes {
		fk := nodes[i].UserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (rpq *ResearchProjectQuery) loadTranslations(ctx context.Context, query *ResearchProjectTranslationQuery, nodes []*ResearchProject, init func(*ResearchProject), assign func(*ResearchProject, *ResearchProjectTranslation)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*ResearchProject)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(researchprojecttranslation.FieldResearchProjectID)
	}
	query.Where(predicate.ResearchProjectTranslation(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(researchproject.TranslationsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ResearchProjectID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "research_project_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (rpq *ResearchProjectQuery) loadDetails(ctx context.Context, query *ResearchProjectDetailQuery, nodes []*ResearchProject, init func(*ResearchProject), assign func(*ResearchProject, *ResearchProjectDetail)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*ResearchProject)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(researchprojectdetail.FieldResearchProjectID)
	}
	query.Where(predicate.ResearchProjectDetail(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(researchproject.DetailsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ResearchProjectID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "research_project_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (rpq *ResearchProjectQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := rpq.querySpec()
	_spec.Node.Columns = rpq.ctx.Fields
	if len(rpq.ctx.Fields) > 0 {
		_spec.Unique = rpq.ctx.Unique != nil && *rpq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, rpq.driver, _spec)
}

func (rpq *ResearchProjectQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(researchproject.Table, researchproject.Columns, sqlgraph.NewFieldSpec(researchproject.FieldID, field.TypeUUID))
	_spec.From = rpq.sql
	if unique := rpq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if rpq.path != nil {
		_spec.Unique = true
	}
	if fields := rpq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, researchproject.FieldID)
		for i := range fields {
			if fields[i] != researchproject.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if rpq.withUser != nil {
			_spec.Node.AddColumnOnce(researchproject.FieldUserID)
		}
	}
	if ps := rpq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := rpq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := rpq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := rpq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (rpq *ResearchProjectQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(rpq.driver.Dialect())
	t1 := builder.Table(researchproject.Table)
	columns := rpq.ctx.Fields
	if len(columns) == 0 {
		columns = researchproject.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if rpq.sql != nil {
		selector = rpq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if rpq.ctx.Unique != nil && *rpq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range rpq.predicates {
		p(selector)
	}
	for _, p := range rpq.order {
		p(selector)
	}
	if offset := rpq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := rpq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ResearchProjectGroupBy is the group-by builder for ResearchProject entities.
type ResearchProjectGroupBy struct {
	selector
	build *ResearchProjectQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rpgb *ResearchProjectGroupBy) Aggregate(fns ...AggregateFunc) *ResearchProjectGroupBy {
	rpgb.fns = append(rpgb.fns, fns...)
	return rpgb
}

// Scan applies the selector query and scans the result into the given value.
func (rpgb *ResearchProjectGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rpgb.build.ctx, ent.OpQueryGroupBy)
	if err := rpgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ResearchProjectQuery, *ResearchProjectGroupBy](ctx, rpgb.build, rpgb, rpgb.build.inters, v)
}

func (rpgb *ResearchProjectGroupBy) sqlScan(ctx context.Context, root *ResearchProjectQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rpgb.fns))
	for _, fn := range rpgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rpgb.flds)+len(rpgb.fns))
		for _, f := range *rpgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rpgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rpgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ResearchProjectSelect is the builder for selecting fields of ResearchProject entities.
type ResearchProjectSelect struct {
	*ResearchProjectQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rps *ResearchProjectSelect) Aggregate(fns ...AggregateFunc) *ResearchProjectSelect {
	rps.fns = append(rps.fns, fns...)
	return rps
}

// Scan applies the selector query and scans the result into the given value.
func (rps *ResearchProjectSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rps.ctx, ent.OpQuerySelect)
	if err := rps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ResearchProjectQuery, *ResearchProjectSelect](ctx, rps.ResearchProjectQuery, rps, rps.inters, v)
}

func (rps *ResearchProjectSelect) sqlScan(ctx context.Context, root *ResearchProjectQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rps.fns))
	for _, fn := range rps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
