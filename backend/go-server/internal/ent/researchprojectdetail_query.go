// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"silan-backend/internal/ent/predicate"
	"silan-backend/internal/ent/researchproject"
	"silan-backend/internal/ent/researchprojectdetail"
	"silan-backend/internal/ent/researchprojectdetailtranslation"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// ResearchProjectDetailQuery is the builder for querying ResearchProjectDetail entities.
type ResearchProjectDetailQuery struct {
	config
	ctx                 *QueryContext
	order               []researchprojectdetail.OrderOption
	inters              []Interceptor
	predicates          []predicate.ResearchProjectDetail
	withResearchProject *ResearchProjectQuery
	withTranslations    *ResearchProjectDetailTranslationQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ResearchProjectDetailQuery builder.
func (rpdq *ResearchProjectDetailQuery) Where(ps ...predicate.ResearchProjectDetail) *ResearchProjectDetailQuery {
	rpdq.predicates = append(rpdq.predicates, ps...)
	return rpdq
}

// Limit the number of records to be returned by this query.
func (rpdq *ResearchProjectDetailQuery) Limit(limit int) *ResearchProjectDetailQuery {
	rpdq.ctx.Limit = &limit
	return rpdq
}

// Offset to start from.
func (rpdq *ResearchProjectDetailQuery) Offset(offset int) *ResearchProjectDetailQuery {
	rpdq.ctx.Offset = &offset
	return rpdq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (rpdq *ResearchProjectDetailQuery) Unique(unique bool) *ResearchProjectDetailQuery {
	rpdq.ctx.Unique = &unique
	return rpdq
}

// Order specifies how the records should be ordered.
func (rpdq *ResearchProjectDetailQuery) Order(o ...researchprojectdetail.OrderOption) *ResearchProjectDetailQuery {
	rpdq.order = append(rpdq.order, o...)
	return rpdq
}

// QueryResearchProject chains the current query on the "research_project" edge.
func (rpdq *ResearchProjectDetailQuery) QueryResearchProject() *ResearchProjectQuery {
	query := (&ResearchProjectClient{config: rpdq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rpdq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rpdq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(researchprojectdetail.Table, researchprojectdetail.FieldID, selector),
			sqlgraph.To(researchproject.Table, researchproject.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, researchprojectdetail.ResearchProjectTable, researchprojectdetail.ResearchProjectColumn),
		)
		fromU = sqlgraph.SetNeighbors(rpdq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTranslations chains the current query on the "translations" edge.
func (rpdq *ResearchProjectDetailQuery) QueryTranslations() *ResearchProjectDetailTranslationQuery {
	query := (&ResearchProjectDetailTranslationClient{config: rpdq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rpdq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rpdq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(researchprojectdetail.Table, researchprojectdetail.FieldID, selector),
			sqlgraph.To(researchprojectdetailtranslation.Table, researchprojectdetailtranslation.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, researchprojectdetail.TranslationsTable, researchprojectdetail.TranslationsColumn),
		)
		fromU = sqlgraph.SetNeighbors(rpdq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ResearchProjectDetail entity from the query.
// Returns a *NotFoundError when no ResearchProjectDetail was found.
func (rpdq *ResearchProjectDetailQuery) First(ctx context.Context) (*ResearchProjectDetail, error) {
	nodes, err := rpdq.Limit(1).All(setContextOp(ctx, rpdq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{researchprojectdetail.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (rpdq *ResearchProjectDetailQuery) FirstX(ctx context.Context) *ResearchProjectDetail {
	node, err := rpdq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ResearchProjectDetail ID from the query.
// Returns a *NotFoundError when no ResearchProjectDetail ID was found.
func (rpdq *ResearchProjectDetailQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = rpdq.Limit(1).IDs(setContextOp(ctx, rpdq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{researchprojectdetail.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (rpdq *ResearchProjectDetailQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := rpdq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ResearchProjectDetail entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ResearchProjectDetail entity is found.
// Returns a *NotFoundError when no ResearchProjectDetail entities are found.
func (rpdq *ResearchProjectDetailQuery) Only(ctx context.Context) (*ResearchProjectDetail, error) {
	nodes, err := rpdq.Limit(2).All(setContextOp(ctx, rpdq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{researchprojectdetail.Label}
	default:
		return nil, &NotSingularError{researchprojectdetail.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (rpdq *ResearchProjectDetailQuery) OnlyX(ctx context.Context) *ResearchProjectDetail {
	node, err := rpdq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ResearchProjectDetail ID in the query.
// Returns a *NotSingularError when more than one ResearchProjectDetail ID is found.
// Returns a *NotFoundError when no entities are found.
func (rpdq *ResearchProjectDetailQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = rpdq.Limit(2).IDs(setContextOp(ctx, rpdq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{researchprojectdetail.Label}
	default:
		err = &NotSingularError{researchprojectdetail.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (rpdq *ResearchProjectDetailQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := rpdq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ResearchProjectDetails.
func (rpdq *ResearchProjectDetailQuery) All(ctx context.Context) ([]*ResearchProjectDetail, error) {
	ctx = setContextOp(ctx, rpdq.ctx, ent.OpQueryAll)
	if err := rpdq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ResearchProjectDetail, *ResearchProjectDetailQuery]()
	return withInterceptors[[]*ResearchProjectDetail](ctx, rpdq, qr, rpdq.inters)
}

// AllX is like All, but panics if an error occurs.
func (rpdq *ResearchProjectDetailQuery) AllX(ctx context.Context) []*ResearchProjectDetail {
	nodes, err := rpdq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ResearchProjectDetail IDs.
func (rpdq *ResearchProjectDetailQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if rpdq.ctx.Unique == nil && rpdq.path != nil {
		rpdq.Unique(true)
	}
	ctx = setContextOp(ctx, rpdq.ctx, ent.OpQueryIDs)
	if err = rpdq.Select(researchprojectdetail.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (rpdq *ResearchProjectDetailQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := rpdq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (rpdq *ResearchProjectDetailQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, rpdq.ctx, ent.OpQueryCount)
	if err := rpdq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, rpdq, querierCount[*ResearchProjectDetailQuery](), rpdq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (rpdq *ResearchProjectDetailQuery) CountX(ctx context.Context) int {
	count, err := rpdq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (rpdq *ResearchProjectDetailQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, rpdq.ctx, ent.OpQueryExist)
	switch _, err := rpdq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (rpdq *ResearchProjectDetailQuery) ExistX(ctx context.Context) bool {
	exist, err := rpdq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ResearchProjectDetailQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (rpdq *ResearchProjectDetailQuery) Clone() *ResearchProjectDetailQuery {
	if rpdq == nil {
		return nil
	}
	return &ResearchProjectDetailQuery{
		config:              rpdq.config,
		ctx:                 rpdq.ctx.Clone(),
		order:               append([]researchprojectdetail.OrderOption{}, rpdq.order...),
		inters:              append([]Interceptor{}, rpdq.inters...),
		predicates:          append([]predicate.ResearchProjectDetail{}, rpdq.predicates...),
		withResearchProject: rpdq.withResearchProject.Clone(),
		withTranslations:    rpdq.withTranslations.Clone(),
		// clone intermediate query.
		sql:  rpdq.sql.Clone(),
		path: rpdq.path,
	}
}

// WithResearchProject tells the query-builder to eager-load the nodes that are connected to
// the "research_project" edge. The optional arguments are used to configure the query builder of the edge.
func (rpdq *ResearchProjectDetailQuery) WithResearchProject(opts ...func(*ResearchProjectQuery)) *ResearchProjectDetailQuery {
	query := (&ResearchProjectClient{config: rpdq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rpdq.withResearchProject = query
	return rpdq
}

// WithTranslations tells the query-builder to eager-load the nodes that are connected to
// the "translations" edge. The optional arguments are used to configure the query builder of the edge.
func (rpdq *ResearchProjectDetailQuery) WithTranslations(opts ...func(*ResearchProjectDetailTranslationQuery)) *ResearchProjectDetailQuery {
	query := (&ResearchProjectDetailTranslationClient{config: rpdq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rpdq.withTranslations = query
	return rpdq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ResearchProjectID uuid.UUID `json:"research_project_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ResearchProjectDetail.Query().
//		GroupBy(researchprojectdetail.FieldResearchProjectID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (rpdq *ResearchProjectDetailQuery) GroupBy(field string, fields ...string) *ResearchProjectDetailGroupBy {
	rpdq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ResearchProjectDetailGroupBy{build: rpdq}
	grbuild.flds = &rpdq.ctx.Fields
	grbuild.label = researchprojectdetail.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ResearchProjectID uuid.UUID `json:"research_project_id,omitempty"`
//	}
//
//	client.ResearchProjectDetail.Query().
//		Select(researchprojectdetail.FieldResearchProjectID).
//		Scan(ctx, &v)
func (rpdq *ResearchProjectDetailQuery) Select(fields ...string) *ResearchProjectDetailSelect {
	rpdq.ctx.Fields = append(rpdq.ctx.Fields, fields...)
	sbuild := &ResearchProjectDetailSelect{ResearchProjectDetailQuery: rpdq}
	sbuild.label = researchprojectdetail.Label
	sbuild.flds, sbuild.scan = &rpdq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ResearchProjectDetailSelect configured with the given aggregations.
func (rpdq *ResearchProjectDetailQuery) Aggregate(fns ...AggregateFunc) *ResearchProjectDetailSelect {
	return rpdq.Select().Aggregate(fns...)
}

func (rpdq *ResearchProjectDetailQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range rpdq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, rpdq); err != nil {
				return err
			}
		}
	}
	for _, f := range rpdq.ctx.Fields {
		if !researchprojectdetail.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if rpdq.path != nil {
		prev, err := rpdq.path(ctx)
		if err != nil {
			return err
		}
		rpdq.sql = prev
	}
	return nil
}

func (rpdq *ResearchProjectDetailQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ResearchProjectDetail, error) {
	var (
		nodes       = []*ResearchProjectDetail{}
		_spec       = rpdq.querySpec()
		loadedTypes = [2]bool{
			rpdq.withResearchProject != nil,
			rpdq.withTranslations != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ResearchProjectDetail).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ResearchProjectDetail{config: rpdq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, rpdq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := rpdq.withResearchProject; query != nil {
		if err := rpdq.loadResearchProject(ctx, query, nodes, nil,
			func(n *ResearchProjectDetail, e *ResearchProject) { n.Edges.ResearchProject = e }); err != nil {
			return nil, err
		}
	}
	if query := rpdq.withTranslations; query != nil {
		if err := rpdq.loadTranslations(ctx, query, nodes,
			func(n *ResearchProjectDetail) { n.Edges.Translations = []*ResearchProjectDetailTranslation{} },
			func(n *ResearchProjectDetail, e *ResearchProjectDetailTranslation) {
				n.Edges.Translations = append(n.Edges.Translations, e)
			}); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (rpdq *ResearchProjectDetailQuery) loadResearchProject(ctx context.Context, query *ResearchProjectQuery, nodes []*ResearchProjectDetail, init func(*ResearchProjectDetail), assign func(*ResearchProjectDetail, *ResearchProject)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ResearchProjectDetail)
	for i := range nodes {
		fk := nodes[i].ResearchProjectID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(researchproject.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "research_project_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (rpdq *ResearchProjectDetailQuery) loadTranslations(ctx context.Context, query *ResearchProjectDetailTranslationQuery, nodes []*ResearchProjectDetail, init func(*ResearchProjectDetail), assign func(*ResearchProjectDetail, *ResearchProjectDetailTranslation)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*ResearchProjectDetail)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(researchprojectdetailtranslation.FieldResearchProjectDetailID)
	}
	query.Where(predicate.ResearchProjectDetailTranslation(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(researchprojectdetail.TranslationsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ResearchProjectDetailID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "research_project_detail_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (rpdq *ResearchProjectDetailQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := rpdq.querySpec()
	_spec.Node.Columns = rpdq.ctx.Fields
	if len(rpdq.ctx.Fields) > 0 {
		_spec.Unique = rpdq.ctx.Unique != nil && *rpdq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, rpdq.driver, _spec)
}

func (rpdq *ResearchProjectDetailQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(researchprojectdetail.Table, researchprojectdetail.Columns, sqlgraph.NewFieldSpec(researchprojectdetail.FieldID, field.TypeUUID))
	_spec.From = rpdq.sql
	if unique := rpdq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if rpdq.path != nil {
		_spec.Unique = true
	}
	if fields := rpdq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, researchprojectdetail.FieldID)
		for i := range fields {
			if fields[i] != researchprojectdetail.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if rpdq.withResearchProject != nil {
			_spec.Node.AddColumnOnce(researchprojectdetail.FieldResearchProjectID)
		}
	}
	if ps := rpdq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := rpdq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := rpdq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := rpdq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (rpdq *ResearchProjectDetailQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(rpdq.driver.Dialect())
	t1 := builder.Table(researchprojectdetail.Table)
	columns := rpdq.ctx.Fields
	if len(columns) == 0 {
		columns = researchprojectdetail.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if rpdq.sql != nil {
		selector = rpdq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if rpdq.ctx.Unique != nil && *rpdq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range rpdq.predicates {
		p(selector)
	}
	for _, p := range rpdq.order {
		p(selector)
	}
	if offset := rpdq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := rpdq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ResearchProjectDetailGroupBy is the group-by builder for ResearchProjectDetail entities.
type ResearchProjectDetailGroupBy struct {
	selector
	build *ResearchProjectDetailQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rpdgb *ResearchProjectDetailGroupBy) Aggregate(fns ...AggregateFunc) *ResearchProjectDetailGroupBy {
	rpdgb.fns = append(rpdgb.fns, fns...)
	return rpdgb
}

// Scan applies the selector query and scans the result into the given value.
func (rpdgb *ResearchProjectDetailGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rpdgb.build.ctx, ent.OpQueryGroupBy)
	if err := rpdgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ResearchProjectDetailQuery, *ResearchProjectDetailGroupBy](ctx, rpdgb.build, rpdgb, rpdgb.build.inters, v)
}

func (rpdgb *ResearchProjectDetailGroupBy) sqlScan(ctx context.Context, root *ResearchProjectDetailQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rpdgb.fns))
	for _, fn := range rpdgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rpdgb.flds)+len(rpdgb.fns))
		for _, f := range *rpdgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rpdgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rpdgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ResearchProjectDetailSelect is the builder for selecting fields of ResearchProjectDetail entities.
type ResearchProjectDetailSelect struct {
	*ResearchProjectDetailQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rpds *ResearchProjectDetailSelect) Aggregate(fns ...AggregateFunc) *ResearchProjectDetailSelect {
	rpds.fns = append(rpds.fns, fns...)
	return rpds
}

// Scan applies the selector query and scans the result into the given value.
func (rpds *ResearchProjectDetailSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rpds.ctx, ent.OpQuerySelect)
	if err := rpds.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ResearchProjectDetailQuery, *ResearchProjectDetailSelect](ctx, rpds.ResearchProjectDetailQuery, rpds, rpds.inters, v)
}

func (rpds *ResearchProjectDetailSelect) sqlScan(ctx context.Context, root *ResearchProjectDetailQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rpds.fns))
	for _, fn := range rpds.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rpds.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rpds.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
